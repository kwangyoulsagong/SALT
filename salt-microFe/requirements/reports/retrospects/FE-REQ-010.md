# FE-REQ-010 회고 — F000 UI (수리 9건 · 관심 종목 탭 · 온보딩 · 투자 화면 레이아웃)

- 작성: 2026-09-22 (머지 후 백필)
- 체크리스트: `requirements/reports/checklists/FE-REQ-010.md`
- 전 영역 기록: 루트 `requirements/reports/checklists/F000-watchlist-tab.md` · `F000-invite-onboarding.md` ·
  `F000-market-table.md` · 회고 `requirements/reports/retrospects/F000-watchlist-tab.md`

## 1. 무엇을 했나

#41 에서 수리 9건 · 관심 종목 탭 · 뉴스 프리뷰, #42 에서 초대 · 온보딩 3스텝, #44 에서 투자 화면
가로 넘침(`decbd23`), #47 에서 세로 높이(FR-53, `0455fac`). 앞서 #39(`b501f6a` 프리뷰 깜빡임)
· #40(`d954e23` 홈 섹션 크래시 · `14c5be4` 조사 · `3a736de` MSW 게이트)이 같은 화면을 고쳤다.

## 2. 잘 된 것

- **두 탭의 별을 같은 쿼리 키로 묶었다.** 동기화 코드 0줄로 FR-34 가 닫혔다.
- **번들 측정이 회귀를 세 번 잡았다.** barrel(`/investments` 125 → 178 kB) · axios 두 번
  (#41 `/home` 158 → 136 kB, #42 `ac86d55` 166 → 145 kB). 전부 기능은 정상이었다.
- **사용자 결정을 뒤집지 않았다.** `05b0427` 에서 표 선택 보라색을 뺀 뒤 `85913fc` 는 선택 표시가
  필요한 자리에서도 색을 다시 칠하지 않고 프리뷰 · `aria-selected` 로 대신했다.

## 3. 틀렸던 것

### 3뷰포트로 반응형을 닫았다 — `decbd23`

09-18 에 FR-51 을 375/390/1440 으로 pass 판정했다. 1280 · 1024 에서 2컬럼 상자가 내용 폭이 되어
페이지가 가로로 밀렸다(1280 문서 폭 1368~1420). 뉴스 제목 · 차트도 같은 이유로 넘쳤다.

> **Action:** 검수를 5뷰포트(1920 · 1440 · 1280 · 1024 · 375)로 넓혔다. 다만 REQ changelog 에만
> 적혔고 **FR-54 행 문구는 아직 "375 / 390 / 1440"** 이다 — 행을 고친다.

### `aria-selected` 가 첫 행에 멈춰 있었다 — `85913fc`

FR-61 을 09-18 에 pass 로 적었지만 실시간 표 행의 `memoKey` 에 선택 상태가 빠져 있었다. 같은
함정이 이미 기록돼 있었다 — 루트 `F000-watchlist-tab.md` §8 "`TableRow` 는 `memoKey` 만 비교한다.
별 상태를 넣지 않으면 눌러도 그 행이 안 그려진다". 별에는 적용하고 선택에는 적용하지 않았다.

> **Action:** `TableRow` 에 넘기는 **모든 표시 상태**를 `memoKey` 에 넣는다 — 컴포넌트 주석이나
> `component-convention.md` 에 한 줄로.

### FR-53 을 "문서 높이 = 뷰포트"로만 판정했다 — `0455fac` → `455d8a5`(후속, 미머지)

`0455fac` 로 1440×900 문서 높이 1195 → 900 을 맞췄다. 그런데 상자가 505px 에 갇혀 표 8줄,
프리뷰는 차트 아래가 잘렸다. 사용자 결정으로 FR-53 을 개정해 헤더 높이만큼 페이지 스크롤을
허용하는 커밋이 `fix/investments-panel-height` 에 있다(`455d8a5`, 2026-09-22 기준 미머지).

> **Action:** 높이 요구는 문서 높이와 함께 **보이는 행 수 · 프리뷰 잘림**을 같이 잰다.

### axios 회귀를 반복했다 — `ac86d55`

watchlist 슬라이스 회고가 적은 실수를 초대 슬라이스가 되풀이했다. 2026-09-22 기준 막는 lint 규칙은
없고, `fsd-shared.md` "`shared/api` 가 BFF 호출의 유일한 경로다"와 달리 `marketApi.ts` ·
`toggleWatchlistApi.ts` 가 axios 를 직접 쓴다. **Action:** `no-restricted-imports` 로 올린다(`FE-REQ-012` 회고와 같은 항목). → **`FE-REQ-035` 에서 닫힘**(2026-09-22)

## 4. 남은 기술부채

| 항목 | 언제 닫히나 |
|---|---|
| 온보딩 화면 · 홈 카드 브라우저 실측 | 루트 `F000-realtime-reliability.md` §5 에 이월된 채 남음 |
| FR-63 `tablist` 아님 (`SegmentedControl` 갭 열림) | `SegmentedControl` 교체 시 |
| 목표 기본 1년 · 카테고리 임시 매핑 · 목표 조회 MSW | PM 판단 · `goal` 이관 · `FE-REQ-012` |
| 관심 목록 조회가 서버 컴포넌트 아님 | `FE-REQ-013` (쿠키) |
| FR-53 오프셋 상수(395, 후속에서 140) | `FE-REQ-030` 자산 탭 이관 시 |
| 2026-09-21 추가 FR-36~38 · 70~78 · 80~87 · 90~93 · 100~107 | 미착수 |
| 웹 테스트 러너 없음 (표 선택 · 기간 전환 자동 테스트 0) | `FE-REQ-013` |

## 5. 다음에 보완할 규칙 · 문서

- **영역 체크리스트가 루트 판정을 따라가지 않았다.** `FE-REQ-010.md` 는 09-18 상태(§3 375/390/1440
  3뷰포트)에 멈춰 있고, FR-50 다르게 · FR-51 정정 · FR-53 pass 는 루트 `F000-market-table.md`
  §3 에만 있다. `pr-convention.md` §2 "체크리스트 리포트가 진실이다"와 어긋난다 — 루트 판정을 바꾸는
  커밋은 영역 체크리스트도 같이 고친다.
- #39 `b501f6a` 는 "브라우저에서 눈으로 확인하지 못했다"를 본문에 적었다. 화면 커밋의 스크린샷
  근거(루트 회고 Action)가 아직 규칙이 아니다.
