# FE-REQ-039 — 회고 (2026-09-24)

- **dev 서버가 도는 중에 `next build` 를 돌려 `.next` 를 엎었다**(메모리 규칙 위반). `/coach/report` 가 500 — 사용자 승인 받고 dev 재시작으로 복구. 빌드 전에 `lsof` 로 3000 을 봤는데도 멈추지 않았다
- 참고 화면 실측 값이 기존 `surface.css.ts`(같은 날 다른 화면 실측)와 헤어라인 · 옅은 면 값까지 같았다 — 실측이 누적되고 있다
- 브라우저에서 처음 본 것 두 가지: 접힘을 `hidden` 속성으로 했더니 `display:flex` 가 덮어 늘 보였고, 전역 글꼴이 없어 라벨이 세리프였다. 둘 다 타입 · 린트 · 빌드로는 안 잡힌다
- axe 가 옅은 면 위 회색 글자(4.26:1)를 잡았다. 흐림을 색으로 하지 않고 굵기로 했다
- 손절 % 프리셋은 기획서 비기능에 있었지만 곱셈이라 뺐다 — 기획 · 규칙이 부딪힌 자리. 서버 프리셋으로 풀린다

## Action

- 빌드 전에 3000 · 3001 리슨 중이면 dev 를 먼저 내리거나 빌드를 미룬다(메모리 `local-fullstack-run` 에 이미 있다 — 이번엔 읽고도 안 지켰다)
- 전역 `body` 글꼴 — 사용자 결정 후 한 줄(`globals.css.ts`)
- `apps/web` 에 vitest 러너 — `shared/lib/amountInput.ts` 같은 순수 함수 테스트 자리
