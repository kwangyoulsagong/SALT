# FE-REQ-026 (F004 UI) — 회고

- 날짜: 2026-09-22 · 슬라이스: `F004-fe-coach-panel` (K절) · 루트 회고: `requirements/reports/retrospects/F004-fe-coach-panel.md`

## 잘 된 것
- `BlockedNotice` 를 오류처럼 보이지 않게(회색 · 경고 아이콘 없음 · 재시도 없음) 두었다 — 초기에 거의 모든 종목이 이 상태다
- 구간 거리를 금액 + "위 / 아래"로만 말한다. % 와 이득 표현이 들어갈 자리가 없다
- 문구 금지어 grep 이 주석에도 걸려 주석을 고쳤다 — 수용 기준이 grep 이면 주석도 대상이다

## 부채 · 다음 행동
- ⑦ 버튼(FR-119)은 L절 페이지와 함께 — 없는 경로로 링크하지 않았다
- `renderable: true` 화면은 실데이터로 본 적이 없다. 표본이 쌓이면 스크린샷 검수(카피: "관찰 구간"이 권유로 읽히는지 — Open Question)
- 패널 세로 길이(Open Question) — 막힌 상태에서는 문제없음. 판단이 열리면 다시 본다

## 슬라이스 6 — L절 상세 분석 (2026-09-22) · 루트 회고: `requirements/reports/retrospects/F004-fe-detail-page.md`

### 잘 된 것
- 코치 카드의 게이트를 새로 만들지 않았다 — 막힘 · 계약 깨짐은 `JudgmentSummary` 가 그대로 그린다
- 맞았던 때 · 틀렸던 때를 한 목록 같은 크기로(B2) — 틀린 사례를 접을 자리를 만들지 않았다
- 해설 카드 안에 성적 · 사례를 같이 둬서 해설만 떼어 읽히지 않는다(FR-135)

### 부채 · 다음 행동
- 주문 전 체크(FR-150~156)는 서버 계약(`stopLossRate` · `maxLossOfTotalRate`)이 먼저다
- 차트 1주 탭(FR-132) — 서버 `week` 후
- 수익 플랜과 "내 규칙 가격" 표가 같은 세 가격을 두 번 보여준다(REQ 가 둘 다 요구). 판단이 열린 화면을 보고 PM 검수
- 캔들이 선 때문에 납작해지는 문제 — 실데이터 화면을 보고 가장자리 표시 여부를 정한다
