# FE-REQ-010 (F000 UI) — 검증 체크리스트

- REQ: `requirements/specs/in-progress/FE-REQ-010-F000-UI.md`
- 브랜치: `feat/f000-watchlist-tab` → `feat/f000-invite-onboarding-slice` · 검증일: 2026-09-18
- 검증일: 2026-09-18
- 상태: **부분 완료** — 수리 9건에 이어 **초대 코드·온보딩 3스텝(FR-20~26·64)** 이 닫혔다
- **전 영역 통합 기록**: `requirements/reports/checklists/F000-watchlist-tab.md`
  (서버·BFF 와 함께 검증한 계약 실측, 번들, 명령 결과가 그쪽에 있다)

## 1. 수리 9건

| FR | 대상 | 결과 | 근거 |
|---|---|---|---|
| FR-1 | 관심 종목 탭 채우기 | **pass** | 탭 클릭 시 3행 렌더. 빈 화면 아님 |
| FR-2 | 하드코딩 시각 | **pass** | `"실시간 오늘 19:30 기준"` 제거 → WS 마지막 수신 시각. 미수신이면 "실시간 수신 대기 중" |
| FR-3 | 뉴스 프리뷰 실데이터 | **pass** | 실기사 제목·요약·출처·"55분 전". 테스트 문자열 렌더 0건 |
| FR-4 | 오타 | **pass** | 홈에 "20% 덜 썼어요" |
| FR-5 | 빈 "주식" 섹션 | **pass** | 비트코인 5,330,750원 +6.62% · 이더리움 4,093,200원 +6.59% · 합계 9,423,950원 |
| FR-6 | 터치 선택 | **pass** | 실시간 표 행에 `onClick` 추가(hover 유지) |
| FR-7 | 반응형 | **pass** | §3 |
| FR-8 | 차트 `period` | **pass** | `miniute` → `minute`. BFF·서버가 모르는 값을 422 로 거부 |
| FR-9 | 목표 저축 submit | **pass** | 제출 → 서버에 1건 생성(E2E). `console.log` 였다 |

## 2. 관심 종목 탭 (FR-30~35) · 뉴스 (FR-40~45) · 접근성 (FR-60~65)

| FR | 결과 | 근거 |
|---|---|---|
| FR-30 | **pass** | 종목명·심볼·현재가·변동률·자산군 배지·별(제거) |
| FR-31 | **pass** | `priceStale` 이면 "지연" 배지 (AAPL) |
| FR-32 | **pass** | 행 선택 → 우측 프리뷰 교체 |
| FR-33 | **pass** | 0건이면 "관심 종목이 없습니다 / 실시간 차트에서 별을 눌러 추가하세요" |
| FR-34 | **pass** | 실시간 탭 별과 **같은 쿼리 키**를 본다. 동기화 코드 0줄 |
| FR-35 | **pass** | 같은 `ChangeRateCell`·`PriceCell` |
| FR-40~45 | **pass** | 실데이터 · `imageUrl` null 이면 영역 미렌더 · 0건 문구 · 상대 시각 · 새 탭+`noopener` · 블록 구조 유지 |
| FR-60 | **pass** | Enter/Space 로 행 선택 (두 표 공통 헬퍼) |
| FR-61 | **pass** | `tabIndex=0` · `aria-selected` |
| FR-62 | **pass** | 손익 부호를 색 + `+`/`−` 문자 (홈 보유 요약) |
| FR-63 | **다르게 구현** | §4 |
| FR-64 | **해당 없음** | `ProgressStepper` 는 온보딩 화면과 함께 온다 |
| FR-65 | **pass** | `비트코인 관심 종목 제거` · `aria-pressed` |

## 3. 반응형 (FR-50~55) — 375 / 390 / 1440 실측

| 뷰포트 | `body` 가로 스크롤 | 배치 | 표 | 프리뷰 |
|---|---|---|---|---|
| 375 | **없음** | 세로 | 자체 스크롤 · 컬럼 5개 유지 · 종목명 한 줄 | 아래로 접힘 (숨김 아님) |
| 390 | **없음** | 세로 | 동일 | 아래로 접힘 |
| 1440 | **없음** | 2컬럼 | 스크롤 없음 | 우측 500px (기존과 동일) |

`maxHeight="800px"` 고정 → `viewport`(80vh). 1000px 높이에서 800px 이라 기존 화면에서 값이
같고 낮은 화면에서만 줄어든다.

## 4. 미충족 · 판단이 REQ와 다른 것

| 항목 | 사유 | 언제 닫히나 |
|---|---|---|
| FR-20~26 초대 코드·온보딩 3스텝 화면 | 이 브랜치의 범위가 아니었다. `InviteCode` 스키마·서버 유스케이스가 선행이다 | `DB-REQ-001` + `SRV-REQ-008` FR-1~7 이 열릴 때 |
| **FR-63 을 `role="tablist"` 로 하지 않았다** | 이 버튼들은 탭이 아니다 — `tabpanel` 이 없고 화면을 바꾸지 않는다. `tablist` 면 스크린리더가 패널 전환을 기대한다. `role="group"`+`aria-pressed` 로 갔다 | REQ 문구를 고치거나 `SegmentedControl` 로 교체할 때 |
| 목표 **목표일 기본 1년** | 폼에 날짜 입력이 없는데 서버가 요구한다. 화면에 한 줄로 노출은 했지만 사용자가 고른 값이 아니다 | PM 이 기본값을 정하거나 폼에 입력을 추가할 때 |
| 목표 **카테고리 매핑 임시** | 화면 6종과 서버 6종이 짝이 맞지 않아 겹치는 셋만 옮기고 나머지는 `other` | `goal` 컨텍스트 이관 |
| 새 목표가 **홈 목록에 안 보인다** | 저장은 서버, 조회는 아직 MSW(`/api/v1/goals`) | `FE-REQ-012` |
| 관심 목록 조회가 **서버 컴포넌트가 아니다** | 토큰이 `localStorage` 라 서버가 읽을 수 없다 | `FE-REQ-013`. 읽는 자리는 `shared/api/authToken.ts` 하나 |
| 프리뷰 차트가 모바일에서 잘린다 | 생성 시점 폭으로 고정. **동작 확인된 변경 금지 코드** | F006 `FE-REQ-030` |
| `grep "faskdljf"·"덜 썻어요"·"miniute"` 이 0 이 아니다 | 셋 다 **무엇을 고쳤는지 설명하는 주석**에만 남아 있다. 렌더 코드에는 0건 | 유지 |

## 5. 명령·번들

`check-types` · `lint`(`--max-warnings 0`) · `build` · `pnpm test:layer-check`(차단 8 · 통과 5)
· `@repo/ui` 17건 — 전부 pass.

| Route | before | after | Δ |
|---|---|---|---|
| `/investments` | 125 kB | **125 kB** | 0 |
| `/home` | 135 kB | 136 kB | +1 |
| `/goals/addgoals` | 125 kB | 138 kB | +13 |
| 공통 청크 | 103 kB | 103 kB | 0 |

번들 회귀를 두 번 만들고 두 번 측정으로 잡았다(barrel · axios). 경위는 통합 체크리스트 §5.

## 6. 변경 금지 목록 확인

홈 목표 카드 · `AnalysisGraph` · `TipsApp` · 프로필 헤더 **그대로**. 실시간 테이블
5컬럼 · 필터 정렬5/순서2/기간7 · 별 아이콘 · 로고 · blink 2초 · `limit=100` · PC 2컬럼 **그대로**.
색 토큰(상승 `#FF2E55` / 하락 `#1677EE`) **그대로** — 표 선택 배경으로 들어갔던 브랜드
보라(`#E5DBFF`)는 팔레트 밖이라 **되돌렸다**. 표 hover 는 토스증권과 같은 `#F2F4F6` 이다.

## 7. 초대 코드 · 온보딩 3스텝 (2026-09-18, `feat/f000-invite-onboarding-slice`)

| FR | 내용 | 결과 | 근거 |
|---|---|---|---|
| FR-20 | 회원가입 화면을 초대 코드 화면으로 대체 | **pass** | **회원가입 화면은 구현된 적이 없었다** — `/signup` 은 `PUBLIC_PATHS` 에 경로만 있었다. 그 자리를 지우고 `/onboarding` 을 넣었다 |
| FR-21 | 3스텝(초대 · 계좌 연결 · 월 적립액), 그 외 질문 없음 | **pass** | `widgets/onboarding-flow`. 뒤 두 단계는 **안내만** |
| FR-22 · 64 | `ProgressStepper` · `aria-current="step"` | **pass** | `@repo/ui` `ProgressStepper` 가 현재 단계에만 준다 |
| FR-23 | 입력 중 `invite/check` · 상한 초과 미노출 | **pass** | 디바운스 400ms · 6자 게이트 · 코드별 캐시 키 |
| FR-24 | 실패 `reasonCode` 3종이 각각 다른 문구 | **pass** | `INVITE_REASON_MESSAGES`. 없는 코드는 다시 입력, 나머지 둘은 새 코드 요청 |
| FR-25 | 온보딩 미완료면 홈에 **카드 1개** | **pass** | `OnboardingCard`. `complete` 면 `null`. 로딩 중에도 렌더하지 않는다(레이아웃 흔들림) |
| FR-26 | 붙여넣기 · 자동 대문자화 · 공백 제거 | **pass** | `normalizeInviteCode` — 서버와 같은 규칙 |

## 8. 이 작업에서 고친 것 — 토큰이 저장되지 않고 있었다

`ACCESS_TOKEN_KEY` 를 **읽는 코드만 있고 쓰는 코드가 없었다.** `useSignIn` 은 `USER_KEY`
만 저장해서, 로그인에 성공해도 `authHeader()` 가 언제나 빈 객체였다. `/api/app/*` 를
부르는 화면(관심 목록 · 포트폴리오 요약 · 온보딩 상태)이 전부 401 이고 화면은 그것을
"데이터 없음"으로 그려서 **빌드·타입체크·lint 를 통과한 채 조용히 깨져 있었다.**

`shared/api/authToken.ts` 에 `writeSession` 을 두고 로그인·초대 수락이 같이 쓴다.
읽기와 쓰기가 한 파일이라 `FE-REQ-013`(쿠키 이관)이 그 파일 하나만 고치면 된다.

## 9. 번들 — 같은 회귀를 반복했다

새 호출 셋에 `axios` 를 써서 `/home` **166 kB** · `/onboarding` **146 kB** 였다. 두 화면
다 axios 를 쓰지 않던 곳이라 GET 하나에 라이브러리가 통째로 들어왔다. `apiFetch` 로
바꿔 **145 · 125 kB** 로 되돌렸다.

**`FE-REQ-009` 회고가 이미 두 번 적은 실수다.** 회고에 적는 것만으로는 다음 세션이 같은
선택을 막지 못한다 — lint 규칙이 될 후보다(`no-restricted-imports` 로 `axios` 를 특정
슬라이스 밖에서 막는 형태).

`/investments` 가 +8 kB 다. `entities/auth` barrel 이 새 쿼리 훅을 re-export 해서
`ProfileHeader` 만 쓰는 화면에도 딸려 오는 것으로 보인다. 예산(40 kB) 안이라 두고
`FE-REQ-013` 에서 다룬다.

## 10. 남은 것

| 항목 | 언제 닫히나 |
|---|---|
| **브라우저 화면 실측** — 온보딩 화면·홈 카드를 눈으로 보지 않았다 | 다음 작업의 첫 항목. 계약 12경우는 `curl` 로 확인했다 |
| 온보딩 2·3단계가 **안내만** 한다 | 연결 화면 F001 · 적립 설정 F003 |
| FR-63 `FilterTabs` 접근성 | §4 (판단 근거는 컴포넌트 주석) |

## 11. FR-53 개정 — 헤더만큼 페이지 스크롤 허용 (2026-09-22, `fix/investments-panel-height`)

사용자가 "높이를 더 늘려야" 한다고 했다. 900px 화면에서 표 · 프리뷰 상자가 505px 이라 표 8줄,
프리뷰는 차트 아래가 잘렸다. 395px 오프셋은 실측과 맞았으므로(상자 위 315 · 아래 80) 계산 오류가
아니라 **"페이지 스크롤 없음"이라는 조건 자체의 비용**이었다. 세 안(아래 여백만 · 스크롤 허용 ·
헤더 축소) 중 사용자가 스크롤 허용을 골랐다.

| 확인 | 결과 |
|---|---|
| 상자 높이 (Playwright) | 1920×1080 **940** · 1440×900 **760** · 1280×720 **580** · 1024×768 **628** |
| 끝까지 스크롤 | 필터 줄 y=29, 상자 아래 여백 80 — 한 화면 |
| 가로 넘침 | 0 (네 폭) · 375 세로 배치 그대로 |
| `tsc --noEmit` · `eslint src/widgets/market-board` | pass |
| 관심 종목 탭 | **실측 안 함** — 머지 전 확인 |
| `pnpm build` · 전체 lint | 안 돌림 — 머지 전 CI |


## FR-53 정정 — 상세에서 돌아오면 보드가 잘렸다 (2026-09-23)

표 + 패널 상자의 **높이는 뷰포트 기준**(`100dvh - 140px`)인데 **위치는 문서 기준**이었다. 페이지가
스크롤된 상태에서는 상자가 화면 밖으로 밀려 표 머리와 패널 위쪽이 사라진다. 상세에서 돌아올 때
브라우저가 스크롤을 복원하면 **항상** 그 상태가 됐다(사용자 보고 — "돌아가면 깨져").

상자를 화면에 붙였다(`position: sticky; top: 0`, 데스크톱만). 헤더는 스크롤로 밀려 올라가고 상자가
화면을 채운다 — 2026-09-22 에 정한 동작과 같고, 스크롤 위치가 어디든 같은 모습이 된다.

미검증: 모바일(768px 미만)은 세로 스택이라 이 경로를 타지 않는다. 실측은 데스크톱에서만 했다.


## 2026-09-23 — 홈: 목 제거 뒤 (`feat/fe-f004-coach-report`)

| 항목 | 판정 | 근거 |
|---|---|---|
| 보유 목록 종목 아이콘 | **pass** | `HoldingSummaryList` — `AssetIcon`(BFF `logoUrl`, 없으면 이니셜). 고정 데이터 실측 로고 2 · 이니셜 1 |
| 목표 목록 아이콘 | **부분** | 화면 6종에 있는 서버 분류(여행 · 첫 차 · 결혼)만 아이콘, 창업 · 창작 · 기타는 아이콘 없음(지어내지 않는다) |
| "지난주 대비 N% 덜 썼어요" | **삭제** | 서버에 지출 데이터 없음 |
| 홈 디자인(참고 화면 기준) | **미착수** | 프로필 동그라미 · 기본 파란 링크 "세금 마감 콕핏" 이 남아 있다 — 다음 슬라이스 |

## 2026-09-24 — 홈 "투자 분석" 블록 → `/investments`

사용자 요청. 블록 전체가 `next/link` — 안에 다른 링크 · 버튼이 없어서 감쌀 수 있다(생기면 머리 줄만 링크로 좁힌다).
Playwright: 블록 클릭 → `/investments`. 포커스 링 · 스크린 리더 이름("투자 분석 — 투자 화면으로 이동").
