# FE-REQ-010 (F000 UI) — 검증 체크리스트

- REQ: `requirements/specs/in-progress/FE-REQ-010-F000-UI.md`
- 브랜치: `feat/f000-watchlist-tab` (커밋 19개, `origin` 푸시됨 · PR 미개설)
- 검증일: 2026-09-18
- 상태: **부분 완료** — 수리 9건과 표시·접근성·반응형은 닫혔고 **초대 코드·온보딩 화면이 남았다**
- **전 영역 통합 기록**: `requirements/reports/checklists/F000-watchlist-tab.md`
  (서버·BFF 와 함께 검증한 계약 실측, 번들, 명령 결과가 그쪽에 있다)

## 1. 수리 9건

| FR | 대상 | 결과 | 근거 |
|---|---|---|---|
| FR-1 | 관심 종목 탭 채우기 | **pass** | 탭 클릭 시 3행 렌더. 빈 화면 아님 |
| FR-2 | 하드코딩 시각 | **pass** | `"실시간 오늘 19:30 기준"` 제거 → WS 마지막 수신 시각. 미수신이면 "실시간 수신 대기 중" |
| FR-3 | 뉴스 프리뷰 실데이터 | **pass** | 실기사 제목·요약·출처·"55분 전". 테스트 문자열 렌더 0건 |
| FR-4 | 오타 | **pass** | 홈에 "20% 덜 썼어요" |
| FR-5 | 빈 "주식" 섹션 | **pass** | 비트코인 5,330,750원 +6.62% · 이더리움 4,093,200원 +6.59% · 합계 9,423,950원 |
| FR-6 | 터치 선택 | **pass** | 실시간 표 행에 `onClick` 추가(hover 유지) |
| FR-7 | 반응형 | **pass** | §3 |
| FR-8 | 차트 `period` | **pass** | `miniute` → `minute`. BFF·서버가 모르는 값을 422 로 거부 |
| FR-9 | 목표 저축 submit | **pass** | 제출 → 서버에 1건 생성(E2E). `console.log` 였다 |

## 2. 관심 종목 탭 (FR-30~35) · 뉴스 (FR-40~45) · 접근성 (FR-60~65)

| FR | 결과 | 근거 |
|---|---|---|
| FR-30 | **pass** | 종목명·심볼·현재가·변동률·자산군 배지·별(제거) |
| FR-31 | **pass** | `priceStale` 이면 "지연" 배지 (AAPL) |
| FR-32 | **pass** | 행 선택 → 우측 프리뷰 교체 |
| FR-33 | **pass** | 0건이면 "관심 종목이 없습니다 / 실시간 차트에서 별을 눌러 추가하세요" |
| FR-34 | **pass** | 실시간 탭 별과 **같은 쿼리 키**를 본다. 동기화 코드 0줄 |
| FR-35 | **pass** | 같은 `ChangeRateCell`·`PriceCell` |
| FR-40~45 | **pass** | 실데이터 · `imageUrl` null 이면 영역 미렌더 · 0건 문구 · 상대 시각 · 새 탭+`noopener` · 블록 구조 유지 |
| FR-60 | **pass** | Enter/Space 로 행 선택 (두 표 공통 헬퍼) |
| FR-61 | **pass** | `tabIndex=0` · `aria-selected` |
| FR-62 | **pass** | 손익 부호를 색 + `+`/`−` 문자 (홈 보유 요약) |
| FR-63 | **다르게 구현** | §4 |
| FR-64 | **해당 없음** | `ProgressStepper` 는 온보딩 화면과 함께 온다 |
| FR-65 | **pass** | `비트코인 관심 종목 제거` · `aria-pressed` |

## 3. 반응형 (FR-50~55) — 375 / 390 / 1440 실측

| 뷰포트 | `body` 가로 스크롤 | 배치 | 표 | 프리뷰 |
|---|---|---|---|---|
| 375 | **없음** | 세로 | 자체 스크롤 · 컬럼 5개 유지 · 종목명 한 줄 | 아래로 접힘 (숨김 아님) |
| 390 | **없음** | 세로 | 동일 | 아래로 접힘 |
| 1440 | **없음** | 2컬럼 | 스크롤 없음 | 우측 500px (기존과 동일) |

`maxHeight="800px"` 고정 → `viewport`(80vh). 1000px 높이에서 800px 이라 기존 화면에서 값이
같고 낮은 화면에서만 줄어든다.

## 4. 미충족 · 판단이 REQ와 다른 것

| 항목 | 사유 | 언제 닫히나 |
|---|---|---|
| FR-20~26 초대 코드·온보딩 3스텝 화면 | 이 브랜치의 범위가 아니었다. `InviteCode` 스키마·서버 유스케이스가 선행이다 | `DB-REQ-001` + `SRV-REQ-008` FR-1~7 이 열릴 때 |
| **FR-63 을 `role="tablist"` 로 하지 않았다** | 이 버튼들은 탭이 아니다 — `tabpanel` 이 없고 화면을 바꾸지 않는다. `tablist` 면 스크린리더가 패널 전환을 기대한다. `role="group"`+`aria-pressed` 로 갔다 | REQ 문구를 고치거나 `SegmentedControl` 로 교체할 때 |
| 목표 **목표일 기본 1년** | 폼에 날짜 입력이 없는데 서버가 요구한다. 화면에 한 줄로 노출은 했지만 사용자가 고른 값이 아니다 | PM 이 기본값을 정하거나 폼에 입력을 추가할 때 |
| 목표 **카테고리 매핑 임시** | 화면 6종과 서버 6종이 짝이 맞지 않아 겹치는 셋만 옮기고 나머지는 `other` | `goal` 컨텍스트 이관 |
| 새 목표가 **홈 목록에 안 보인다** | 저장은 서버, 조회는 아직 MSW(`/api/v1/goals`) | `FE-REQ-012` |
| 관심 목록 조회가 **서버 컴포넌트가 아니다** | 토큰이 `localStorage` 라 서버가 읽을 수 없다 | `FE-REQ-013`. 읽는 자리는 `shared/api/authToken.ts` 하나 |
| 프리뷰 차트가 모바일에서 잘린다 | 생성 시점 폭으로 고정. **동작 확인된 변경 금지 코드** | F006 `FE-REQ-030` |
| `grep "faskdljf"·"덜 썻어요"·"miniute"` 이 0 이 아니다 | 셋 다 **무엇을 고쳤는지 설명하는 주석**에만 남아 있다. 렌더 코드에는 0건 | 유지 |

## 5. 명령·번들

`check-types` · `lint`(`--max-warnings 0`) · `build` · `pnpm test:layer-check`(차단 8 · 통과 5)
· `@repo/ui` 17건 — 전부 pass.

| Route | before | after | Δ |
|---|---|---|---|
| `/investments` | 125 kB | **125 kB** | 0 |
| `/home` | 135 kB | 136 kB | +1 |
| `/goals/addgoals` | 125 kB | 138 kB | +13 |
| 공통 청크 | 103 kB | 103 kB | 0 |

번들 회귀를 두 번 만들고 두 번 측정으로 잡았다(barrel · axios). 경위는 통합 체크리스트 §5.

## 6. 변경 금지 목록 확인

홈 목표 카드 · `AnalysisGraph` · `TipsApp` · 프로필 헤더 **그대로**. 실시간 테이블
5컬럼 · 필터 정렬5/순서2/기간7 · 별 아이콘 · 로고 · blink 2초 · `limit=100` · PC 2컬럼 **그대로**.
색 토큰(상승 `#FF2E55` / 하락 `#1677EE`) **그대로** — 표 선택 배경으로 들어갔던 브랜드
보라(`#E5DBFF`)는 팔레트 밖이라 **되돌렸다**. 표 hover 는 토스증권과 같은 `#F2F4F6` 이다.
