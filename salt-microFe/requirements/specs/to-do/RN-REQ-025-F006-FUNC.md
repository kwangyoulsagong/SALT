---
id: RN-REQ-025
feature: F006
area: rn
kind: FUNC
title: "F006 코치 대화 & 3탭 IA — 모바일 기능 규칙 정의 (SSE 백그라운드 · 재연결)"
priority: critical
labels: [rn, func, fsd, sse, background, reconnect, offline]
created: 2026-09-09
---

> **2026-09-21 개정.** `ADR-002` — 홈 블록 순서(FR-32)가 3블록으로 바뀌었다. 거래 기록 · 알림 읽음 · 설정 · 온보딩 건너뛰기의 모바일 규칙(오프라인 쓰기 금지 포함)을 추가했다(§2026-09-21). 근거: 스토리보드 갭 감사 D5 · D6 · D9 · B12 · B13 · B14.

## Summary

모바일 대화의 핵심 규칙은 **백그라운드 전환 처리**다. 앱이 백그라운드로 가면 스트림이 끊기고, 복귀 시 **중복 생성 없이** 이어야 한다.

## SSE — 백그라운드가 웹과 다른 점

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | **`EventSource`가 RN에 기본 내장되어 있지 않다.** 폴리필 또는 `fetch` + `ReadableStream` 직접 구현을 쓴다 | Must |
| FR-2 | 직접 구현이면 **헤더에 토큰을 붙일 수 있다.** 웹의 쿠키 문제가 없다 | Must |
| FR-3 | 앱이 **백그라운드로 가면 스트림을 끊는다.** OS가 곧 끊고, 남겨두면 배터리를 먹는다 | Must |
| FR-4 | 복귀 시 **같은 `messageId`로 재연결**한다. `Last-Event-ID`로 이어받는다 | Must |
| FR-5 | **재연결 시 `POST /messages`를 다시 부르지 않는다.** 메시지가 두 개 생긴다 | Must |
| FR-6 | 복귀 시 이미 완료됐으면 **전문을 한 번에** 받고 닫는다 | Must |
| FR-7 | 재연결 백오프(1s → 2s → 4s). 3회 실패 시 끊고 재시도 버튼 | Must |
| FR-8 | 언마운트·취소 시 스트림을 닫고 **서버로 종료를 전파**한다 | Must |
| FR-9 | 중복 델타를 방어적으로 검사한다 | Must |

## 스트리밍 반영

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-10 | 델타를 **마지막 말풍선에만** 누적한다 | Must |
| FR-11 | 토큰을 **프레임 단위로 버퍼링**한다. **JS 스레드가 하나이므로 웹보다 중요하다** | Must |
| FR-12 | 카드는 완성 객체로 한 번에 붙인다 | Must |
| FR-13 | `done`에서 스트리밍 해제 + 배지 결정 | Must |
| FR-14 | `error`에서 `fallbackText`를 렌더한다. 빈 화면 0건 | Must |
| FR-15 | 자동 스크롤이 **매 델타마다 일어나지 않게** 프레임 단위로 | Must |

## 오프라인

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-20 | 오프라인이면 **스트림을 시작하지 않는다.** 입력을 보존하고 안내한다 | Must |
| FR-21 | 오프라인에서 과거 대화는 **캐시로 볼 수 있다** | Must |
| FR-22 | **오프라인 큐 0건.** 나중에 자동 전송하지 않는다 | Must |
| FR-23 | 홈은 캐시 + "오프라인 · {시각} 기준" 배너 | Must |
| FR-24 | 온라인 복귀 시 조회만 재개. 자동 전송 0건 | Must |

## 홈 — 읽기 전용

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-30 | **mutation 호출 0건** | Must |
| FR-31 | 블록을 누르면 해당 탭으로 이동 | Must |
| FR-32 | 블록 순서를 코드로 고정: 총자산 → 적립 → 추천 → 기존 목표 · 투자 요약 · 팁. **개정 2026-09-21** (D6) | Must |
| FR-33 | 금액 `null`을 `0`으로 표시하지 않는다 | Must |
| FR-34 | AI 추천에 게이트 적용 | Must |
| FR-35 | 온보딩 미완료면 블록 대신 카드 | Must |
| FR-36 | Pull-to-refresh가 조회만 한다 | Should |

## 딥링크

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-40 | 알림 딥링크가 해당 탭·화면으로 진입한다. `target` 코드 매핑: `coach` → 코치 탭, `home.weekly-plan` → 홈 적립 블록 (D5) | Must |
| FR-41 | **딥링크 파라미터를 신뢰하지 않고** 진입 후 서버 재검증 | Must |
| FR-42 | 미로그인 딥링크는 로그인 후 이어진다 | Must |
| FR-43 | 뒤로가기가 앱을 닫지 않게 스택을 구성한다 | Must |

## 문구

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-50 | 사용자 문구는 `shared/i18n`. **추천 질문 4개도 여기** | Must |
| FR-51 | 키를 웹과 공유(`packages/core`), 값은 플랫폼별 | Must |
| FR-52 | 금지 표현 검사를 카탈로그에 적용 | Must |
| FR-53 | 서버 코드를 i18n 키로 매핑. 매핑 없으면 렌더하지 않는다 | Must |
| FR-54 | 면책을 답변마다 붙인다 | Must |

## 주문 미실행

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-60 | 대화 화면에 **"주문하기" 버튼·거래소 앱 딥링크가 0건**이다 | Must |
| FR-61 | `Linking.openURL`로 거래소 앱을 열지 않는다 | Must |

## 상태 관리

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-70 | 조회는 React Query. `staleTime` 명시 | Must |
| FR-71 | `POST /messages` mutation **재시도 0회** | Must |
| FR-72 | 스트림 완료 후 메시지 목록을 갱신한다 | Must |
| FR-73 | **낙관적 갱신 0건**(대화는 서버 응답이 내용이다) | Must |
| FR-74 | 백그라운드 복귀 시 홈이 stale이면 재조회 | Must |
| FR-75 | 대화 삭제는 `Dialog.confirm`. 자동 삭제 0건 | Must |

## 2026-09-21 추가

### 거래 기록 (B14)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-80 | 미리보기는 입력 디바운스 300ms + 이전 요청 취소. **평단을 앱에서 계산하지 않는다** **(기본안 — 감사 문서 B14)** | Must |
| FR-81 | 저장 · 수정 · 삭제는 **온라인에서만**. 오프라인이면 버튼 비활성 + 사유. **오프라인 큐 0건** — 나중에 자동 저장하지 않는다 | Must |
| FR-82 | 쓰기 재시도 0회 · 낙관적 갱신 0건. 성공 후 포지션 · 성과 · 거래 목록 · 홈 쿼리 무효화 | Must |
| FR-83 | 과매도 코드는 수량 필드 에러. 입력 보존 | Must |
| FR-84 | 시트가 열린 채 백그라운드로 가도 **입력을 보존**한다 | Must |

### 포지션 (B13)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-90 | 섹션별 `status`로 독립 렌더. `null` 금액 · 레이더 값을 0으로 그리지 않는다 | Must |
| FR-91 | 기간 버튼은 성과 쿼리만 바꾼다 | Must |
| FR-92 | 오프라인이면 마지막 포지션 캐시 + "오프라인 · {시각} 기준" | Must |

### 알림 · 설정 · 온보딩 (D5 · D9 · B12 · B16)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-100 | 알림 읽음 · 모두 읽음은 낙관적 반영 + 실패 시 되돌림(금액이 아니라서 예외). 오프라인이면 누를 수 없다 | Must |
| FR-101 | 벨 배지는 앱 포그라운드 복귀 · 탭 전환 · 알림 화면 이탈 시 갱신. 폴링 0건 | Must |
| FR-102 | 설정은 그룹별 저장, 서버 응답 후 반영. 밴드 임계값 입력 0건 (D9) | Must |
| FR-103 | 적립 기본액 저장 후 **서버 주간 환산값**으로 표시 갱신 | Must |
| FR-104 | 온보딩 [건너뛰기]는 서버 성공 후에만 다음 단계. 3단계는 F003 적립 설정 저장 **(기본안 — 감사 문서 B12)** | Must |
| FR-105 | 온보딩 단계 키 `first_holding`을 `packages/core`에서 받는다. `link_account` 0건 — **BREAKING** | Must |

## Acceptance Criteria

- [ ] SSE 구현 방식이 확정되고 **헤더로 토큰을 보낸다**
- [ ] **백그라운드 전환 시 스트림이 끊긴다**
- [ ] 복귀 시 같은 `messageId`로 재연결되고 `Last-Event-ID`로 이어받는다
- [ ] **재연결 시 `POST /messages`가 다시 호출되지 않는다** (메시지 중복 0건)
- [ ] 복귀 시 완료됐으면 전문을 한 번에 받는다
- [ ] 재연결 백오프가 동작하고 3회 실패 시 버튼이 나온다
- [ ] 언마운트·취소 시 서버로 종료가 전파된다
- [ ] 중복 델타가 걸러진다
- [ ] **델타가 마지막 말풍선에만 반영된다**
- [ ] 토큰이 프레임 단위로 버퍼링된다
- [ ] 카드가 완성 객체로 붙는다
- [ ] `error`에서 `fallbackText`가 렌더된다
- [ ] 자동 스크롤이 프레임 단위다
- [ ] **오프라인에서 스트림이 시작되지 않고 입력이 보존된다**
- [ ] 오프라인에서 과거 대화가 캐시로 보인다
- [ ] **오프라인 큐가 0건이다**
- [ ] 홈 오프라인 배너가 있다
- [ ] 온라인 복귀 시 자동 전송이 0건이다
- [ ] **홈에 mutation 호출이 0건이다**
- [ ] 블록 순서가 코드로 고정된다
- [ ] 금액 `null`이 `0`으로 표시되지 않는다
- [ ] AI 추천에 게이트가 적용된다
- [ ] 딥링크가 재검증되고 뒤로가기가 앱을 닫지 않는다
- [ ] 추천 질문 4개가 `shared/i18n`에 있다
- [ ] 금지 표현 검사가 동작한다
- [ ] 매핑 없는 코드가 렌더되지 않는다
- [ ] 면책이 답변마다 붙는다
- [ ] **"주문하기" 버튼·`Linking.openURL` 거래소 링크가 0건이다**
- [ ] `POST /messages` 재시도가 0건이다
- [ ] 낙관적 갱신이 0건이다
- [ ] 백그라운드 복귀 시 홈이 재조회된다
- [ ] 대화 삭제 시 confirm이 뜨고 자동 삭제가 0건이다
- [ ] 미리보기가 디바운스 + 취소이고 앱에 평단 계산 코드가 0건이다
- [ ] 오프라인에서 거래 쓰기가 막히고 오프라인 큐가 0건이다
- [ ] 거래 쓰기 재시도 · 낙관적 갱신이 0건이다
- [ ] 시트 입력이 백그라운드 복귀 후 보존된다
- [ ] 포지션 `null`이 0으로 그려지지 않는다
- [ ] 알림 읽음이 낙관적 + 되돌림이고 벨 폴링이 0건이다
- [ ] 알림 딥링크 `target` 매핑이 동작한다
- [ ] 설정 그룹별 저장 · 밴드 임계값 입력 0건
- [ ] 온보딩 건너뛰기가 서버 성공 후에만 넘어간다

## Dependencies

- **선행:** `RN-REQ-024`(UI) · `RN-REQ-001`(`packages/core`) · `BFF-REQ-028`
- **규칙:** `rn-architecture.md` · `i18n-policy.md`

## Open Questions

- **RN에서 SSE를 무엇으로 구현할지.** `react-native-sse` 폴리필 vs `fetch` + `ReadableStream`. **RN의 `fetch`가 스트림을 지원하는지** 확인 필요(Hermes/네이티브 네트워킹 구현에 따라 다르다) — **착수 전 필수 조사.**
- 백그라운드 전환 시 스트림을 끊으면 **긴 답변이 중간에 멈춘다.** 복귀 시 이어받지만 체감이 어떤지 확인 필요.
- iOS 백그라운드 짧은 유예(수 초)를 활용해 마무리할지. 복잡도가 크다 → **끊는 것이 기본안.**
- 알림 읽음 낙관적 반영(FR-100)은 "낙관적 갱신 0건"(FR-73)의 예외다. 대화 · 금액이 아니라 표시 상태라 허용했다.

## Changelog

| 날짜 | 변경 |
|---|---|
| 2026-09-21 | ADR-002 머리 배너. 개정: FR-32(블록 순서 D6) · FR-40(`target` 매핑 D5). 추가: FR-80~84(거래 기록 · 오프라인 쓰기 금지 B14) · FR-90~92(포지션 B13) · FR-100~105(알림 D5 · 설정 D9 · B16 · 온보딩 B12, **BREAKING** 단계 키). 근거: `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` · `ADR-002` |
