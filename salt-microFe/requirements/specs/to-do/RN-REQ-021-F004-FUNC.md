---
id: RN-REQ-021
feature: F004
area: rn
kind: FUNC
title: "F004 AI 코치 추천 — 모바일 기능 규칙 정의"
priority: critical
labels: [rn, func, fsd, render-gate, offline, cooldown]
created: 2026-09-09
---

## Summary

모바일 코치의 동작 규칙. 게이트·금지 표현·주문 미실행은 **웹과 완전히 같고 `packages/core`에서 공유**한다. 다른 것은 오프라인과 백그라운드 처리다.

## 게이트 — 웹과 공유

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | 게이트 함수를 **`packages/core`에서 웹과 공유**한다 | Must |
| FR-2 | 서버 `renderable`을 쓰고, **방어적으로 재확인**한다(3종 중 하나가 없으면 렌더하지 않고 개발 로그) | Must |
| FR-3 | **우회 prop·플래그가 0건이다** | Must |
| FR-4 | `disclaimer` 없으면 화면 렌더 안 함. `scoreNote` 없으면 점수 렌더 안 함 | Must |
| FR-5 | `blockedReason` 3종을 i18n 키로 매핑 | Must |

## 계산하지 않는다

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-10 | **점수·승률·MDD·`distancePct`·preflight를 계산하지 않는다** | Must |
| FR-11 | 표시 포맷터를 `packages/core`에서 웹과 공유한다 | Must |
| FR-12 | `staleHours`는 서버 값을 쓴다. **기기 시계로 재계산하지 않는다** — 시계가 틀린 기기에서 배지가 잘못 뜬다 | Must |

## 문구 — 웹과 같은 정책

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-20 | 사용자 문구는 `shared/i18n`. **키를 웹과 공유**하고 값은 플랫폼별(모바일이 짧다) | Must |
| FR-21 | 금지 표현 검사 11개: "확실"·"무조건"·"보장"·"100%"·"목표주가"·"예상 수익률"·"당신은"·"패닉셀러"·"실수"·"잘못"·"점수가 높으니" | Must |
| FR-22 | 서버 코드 4종(`factCode`·`conditionCode`·`reasonCode`·`blockedReason`)을 i18n 키로 매핑 | Must |
| FR-23 | 매핑 없는 코드를 원문으로 노출하지 않는다 | Must |
| FR-24 | 행동 기록을 **사실 서술로만** | Must |
| FR-25 | 익절 문구에 "목표주가"·"예상"을 쓰지 않는다 | Must |

## 주문을 실행하지 않는다

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-30 | **"주문하기"·"매수하기"·"매도하기" 버튼 0건** | Must |
| FR-31 | preflight 끝에 "직접 주문하세요" | Must |
| FR-32 | **거래소 앱 딥링크·`Linking.openURL` 0건.** 모바일에서는 특히 유혹이 크다(앱이 설치되어 있다) | Must |
| FR-33 | preflight에 차단 로직 0건 | Must |

## 쿨다운

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-40 | 쿨다운은 **서버 판정**. 기기 타이머를 신뢰하지 않는다 | Must |
| FR-41 | `retryAfterSeconds`로 로컬 카운트다운하되, 0이 되면 `generation-status`를 재조회한다 | Must |
| FR-42 | **앱이 백그라운드에 있던 동안 타이머가 멈춘다.** 복귀 시 `generation-status`를 재조회한다 | Must |
| FR-43 | 429를 받으면 타이머를 갱신한다. 오류 토스트 0건 | Must |
| FR-44 | 재생성 mutation 재시도 0회 | Must |
| FR-45 | 202 후 폴링. **중단 조건 5개**: 완료 · 실패 · 언마운트 · 최대 횟수 · **백그라운드 전환** | Must |

## LLM 경로

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-50 | **`explain`을 자동 호출하지 않는다.** 버튼 클릭만 | Must |
| FR-51 | `AbortSignal`로 화면 이탈·백그라운드 전환 시 취소한다 | Must |
| FR-52 | 진행 중 재클릭을 막는다 | Must |
| FR-53 | 실패 시 `detail`의 규칙 기반 문장을 보여준다. 오류 화면 0건 | Must |
| FR-54 | **모바일 네트워크에서 20s는 길다.** 진행 표시를 명확히 하고 취소 버튼을 준다 | Must |

## 피드백

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-60 | `[도움 안 됨]`은 사유 선택 후 전송. 사유 없이 보내지 않는다 | Must |
| FR-61 | 낙관적 갱신 허용. 실패 시 되돌리고 토스트 | Must |
| FR-62 | mutation 재시도 0회 | Must |
| FR-63 | 이미 남긴 피드백을 변경 가능하게(upsert) | Must |

## 오프라인

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-70 | 마지막 추천을 캐시. 키는 `(userId)` | Must |
| FR-71 | 오프라인에서 캐시 + **"오프라인 · {시각} 기준"** 배너 | Must |
| FR-72 | 오프라인에서 mutation(재생성·피드백·preflight·성향) 버튼 비활성 + 사유 | Must |
| FR-73 | **오프라인 큐 0건** | Must |
| FR-74 | 캐시 24시간 초과 시 "오래되었습니다". `staleHours`와 **별개 개념**임을 문구로 구분한다 | Must |
| FR-75 | 온라인 복귀 시 조회만 재개. mutation 자동 재전송 0건 | Must |

## 상태 관리

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-80 | 조회는 React Query(RSC 없음). `staleTime` 명시 | Must |
| FR-81 | preflight는 **버튼 클릭 시** 호출. 입력마다 부르지 않는다 | Must |
| FR-82 | 성향 저장 후 코치 쿼리를 **전부 무효화** | Must |
| FR-83 | 낙관적 갱신은 **피드백만** | Must |
| FR-84 | 백그라운드 복귀 시 stale이면 재조회 | Must |
| FR-85 | ~~선택된 종목·모드를 화면 로컬 상태로~~ **개정 2026-09-21 (D3)**: 종목은 화면 로컬, **모드는 시장 목록 ↔ 상세 화면 공유 상태**다(FR-90) | Must |

## 종목 판단 — 상세 분석 화면 규칙 (2026-09-21)

근거: `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` D2 · D3 · D4 · B1 · B3 · B9 · B10 · B16 · B18.
웹 `FE-REQ-027` FR-80~95 와 **같은 규칙**이고 `packages/core` 에서 공유한다. 다른 것은 모드 상태의 저장소다(RN 에는 URL 이 없다).

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-90 | **모드는 navigation param `mode` 로 들고 다닌다.** 시장 목록 → `AssetDetailScreen` 으로 넘기고, 상세에서 바꾸면 돌아갈 때 목록에 반영한다(`navigation.setParams`). 전역 store · `AsyncStorage` 에 따로 두지 않는다 | Must |
| FR-91 | param 이 없으면 응답의 `mode`(서버가 `defaultMode` 반영 — B16). 앱이 `scalp` 를 박지 않는다. 모드 스위치는 프로필을 저장하지 않는다 | Must |
| FR-92 | 모드 전환은 **요청 0건**이다(두 모드가 한 응답에 있다) | Must |
| FR-93 | 게이트는 **모드별**이다. 방어적 재확인(FR-2)을 모드마다 한다 | Must |
| FR-94 | **신뢰도 · 점수 `%` 표기 0건(D3).** 유효시간은 `validity.code` → i18n 하나. "25분"·"30일" 상수 0건 | Must |
| FR-95 | 구간 · 오버레이 · 거리 · 게이지 적중률 **계산 0건.** `예측 아님` 배지를 끄는 prop 0건 | Must |
| FR-96 | 금지 표현에 "매수존" · "매수 적정가" · "목표가" · "상승 여력" · "신뢰도" · "기대 수익" 6개를 더한다(웹과 같은 17개) | Must |
| FR-97 | 해설 버튼은 `modes[mode].renderable === true` 일 때만. 응답 `{ renderable: false }` 는 `BlockedNotice` 문구 | Must |
| FR-98 | 주문 전 체크: 목표가 초기값 빈칸 · 손절 칩은 `stopLossRate` 전송 · "전액" · 잔여 현금 0건(B1) | Must |
| FR-99 | 시장 · 관심 종목 목록이 코치 뷰모델을 읽지 않는다(D4) | Must |
| FR-100 | 코치 리포트 전부 미렌더(B18)는 정상 분기 — 오류 토스트 · 에러 화면 0건 | Must |
| FR-101 | 오프라인에서 상세 화면은 **마지막 판단을 보여주되 모드 전환은 허용**한다(두 모드가 캐시에 있다). 해설 · 주문 전 체크 · 관심 추가는 비활성 + 사유(FR-72) | Must |

## Acceptance Criteria

- [ ] 게이트 함수가 `packages/core`에서 웹과 공유된다
- [ ] 방어적 재확인이 동작한다 (3종 중 하나 없으면 렌더 안 함 + 개발 로그)
- [ ] **우회 prop·플래그가 0건이다**
- [ ] `disclaimer`·`scoreNote` 없으면 렌더되지 않는다
- [ ] **점수·승률·MDD·`distancePct`·preflight 계산 코드가 0건이다**
- [ ] 포맷터가 웹과 공유된다
- [ ] `staleHours`가 서버 값이다 (기기 시계 재계산 0건)
- [ ] i18n 키가 웹과 공유되고 금지 표현 검사 11개가 동작한다
- [ ] 매핑 없는 코드가 원문으로 노출되지 않는다
- [ ] 행동 기록이 사실 서술이고 인격 평가가 0건이다
- [ ] 익절 문구에 "목표주가"·"예상"이 0건이다
- [ ] **"주문하기" 버튼과 거래소 딥링크·`Linking.openURL`이 0건이다**
- [ ] preflight에 차단 로직이 0건이다
- [ ] 쿨다운이 서버 판정이고 백그라운드 복귀 시 재조회된다
- [ ] 429가 타이머 갱신이고 오류 토스트가 0건이다
- [ ] **폴링 중단 조건 5개가 있다** (백그라운드 포함)
- [ ] **`explain`이 자동 호출되지 않는다**
- [ ] `explain`에 취소 버튼이 있고 백그라운드 전환 시 취소된다
- [ ] 진행 중 재클릭이 막힌다
- [ ] `explain` 실패 시 규칙 문장이 표시되고 오류 화면이 0건이다
- [ ] `[도움 안 됨]`이 사유 없이 전송되지 않는다
- [ ] 피드백이 낙관적 갱신이고 실패 시 되돌아간다
- [ ] 오프라인 배너 + mutation 버튼 비활성
- [ ] **오프라인 큐가 0건이다**
- [ ] 캐시 24시간과 `staleHours`가 문구로 구분된다
- [ ] 온라인 복귀 시 mutation 자동 재전송이 0건이다
- [ ] preflight가 버튼 클릭으로만 호출된다
- [ ] 성향 저장 후 코치 쿼리가 무효화된다
- [ ] 낙관적 갱신이 피드백만이다
- [ ] 백그라운드 복귀 시 stale 데이터가 재조회된다
- [ ] 모드가 navigation param 으로 목록 ↔ 상세를 오가고 store · `AsyncStorage` 에 0건이다
- [ ] 앱 코드에 `scalp` 하드코딩 기본값이 0건이다
- [ ] 모드 전환 요청이 0건이다
- [ ] 게이트가 모드별이다
- [ ] **신뢰도 · 점수 `%` · 유효시간 상수가 0건이다**
- [ ] 구간 · 거리 · 적중률 계산 코드가 0건이다
- [ ] 금지 표현 검사가 17개다
- [ ] 판단 미렌더 모드에서 해설 버튼이 없다
- [ ] 목표가 빈칸 · `stopLossRate` 전송 · "전액" 0건
- [ ] 목록이 코치 뷰모델을 읽지 않는다 (D4)
- [ ] 리포트 전부 미렌더가 오류로 표시되지 않는다
- [ ] 오프라인 상세에서 모드 전환은 되고 mutation 은 비활성이다

## Dependencies

- **선행:** `RN-REQ-020`(UI) · `RN-REQ-001`(`packages/core`) · `BFF-REQ-024`
- **규칙:** `rn-architecture.md` · `i18n-policy.md`

## Open Questions

- 캐시 24시간과 `staleHours`(생성 후 경과)를 사용자가 혼동할 수 있다. **문구를 어떻게 구분할지** 카피 검토 필요.
- `explain` 20s를 모바일에서 기다리게 할지. **취소 버튼 + 진행 표시**가 기본안이지만, 아예 만들지 않는 것도 검토 대상(`FE-REQ-029` Open Question).
- i18n 값을 플랫폼별로 두면 금지 표현 검사를 두 카탈로그에 각각 돌려야 한다.
- 모드를 navigation param 으로만 두면 앱을 다시 열 때 `defaultMode` 로 돌아간다. 마지막 모드를 기억할지는 D3 이 정하지 않았다 — 기억하지 않는 것이 기본안(웹과 같음).

## Changelog

| 날짜 | 변경 |
|---|---|
| 2026-09-21 | `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` 반영. 신규 FR-90~101(모드 navigation param 공유(D3) · `defaultMode` 위임(B16) · 모드 전환 무요청 · 모드별 게이트(B10) · 신뢰도/유효시간 상수 금지 · 무계산(D2 · B9) · 금지 표현 6개 · 해설 게이트(B3) · 주문 전 체크(B1) · 목록 분리(D4) · 초기 미렌더 정상(B18) · 오프라인 모드 전환). 개정: FR-85 |
