---
id: FE-REQ-030
feature: F006
area: fe
kind: UI
title: "F006 코치 대화 & 3탭 IA — 웹 UI 정의 (3탭 · 홈 3블록 · 대화 · 포지션 · 알림 · 설정 · PC 이동식 격자)"
priority: critical
labels: [fe, ui, fsd, ia, conversation, movable-grid, a11y]
created: 2026-09-09
---

> **2026-09-21 개정.** `ADR-002` · 스토리보드 갭 감사 반영. 홈 **3블록 + 기존 블록 그대로**(D6), 자산 탭 = `포지션` / `시장`(지금 `/investments`) / `관심 종목`(D7), `/tax` 진입 링크 제거(zone은 Q1), 알림 1종 화면(D5), 설정 한 화면(D9), 포지션 · 거래 기록 시트 · `RadarChart`(B13 · B14), 온보딩 2단계 교체(B12)를 추가했다. 근거: 스토리보드 갭 감사 D4~D9 · B6 · B7 · B12~B14 · B16 · B19.

## Summary

**IA를 3탭으로 확정하고 대화를 제품의 중심에 놓는다.** PC는 탭이 아니라 `MovableGrid`로 여러 패널을 동시에 본다.

## FSD 배치

| 레이어 | 슬라이스 | 컴포넌트 |
|---|---|---|
| `pages` | `home` · `coach` · `assets` | 3탭 |
| `widgets` | `home-briefing` | 3블록 + 기존 블록(목표 · 투자 요약 · 팁) + `BlockBoundary` *(개정 2026-09-21)* |
| `widgets` | `coach-console` | 대화 + 추천 카드 + 성적표 |
| `widgets` | `asset-workspace` | 세그먼트 헤더 3(`포지션`/`시장`/`관심 종목`) *(개정 2026-09-21, D7)* |
| `widgets` | `position-overview` | Hero · 평가금 흐름 · 보유 목록 · 리스크 레이더 *(2026-09-21)* |
| `widgets` | `market-board`(기존) | `시장` 세그먼트 — 지금 2컬럼 그대로. 우측 패널 내용은 F004 *(2026-09-21)* |
| `widgets` | `app-header` · `alert-list` · `settings-panel` · `transaction-list` | 공통 헤더(프로필 · 검색 · 벨) · 알림 · 설정 · 거래 목록 *(2026-09-21)* |
| `widgets` | `pc-panel-grid` | `MovableGrid` 배치 |
| `features` | `ask-coach` | 메시지 전송 + SSE 구독 |
| `features` | `arrange-panels` | 격자 배치 변경·저장 |
| `features` | `record-transaction` | 거래 기록 시트(추가 · 수정 · 삭제) + 서버 미리보기 *(2026-09-21)* |
| `features` | `mark-alert-read` · `toggle-alerts` · `edit-plan-settings`(F003 소유 — 2026-09-21 대조로 통일) | 읽음 · 모두 읽음 · 알림 켜기/끄기 · 적립 기본액(F003 쓰기) *(2026-09-21)* |
| `features` | `skip-onboarding-step` | 온보딩 2단계 건너뛰기 *(2026-09-21)* |
| `entities` | `coach` | `ChatBubble` 래퍼 · `RecommendationCard`(F004 재사용) · `SuggestedQuestionChips` |
| `entities` | `portfolio`·`plan` | 각 블록 · 포지션 표시 컴포넌트(`RiskRadar`) *(개정 2026-09-21 — `tax`·`invoice` 삭제, ADR-002)* |
| `entities` | `notification` | 알림 행 · 벨 배지 *(2026-09-21)* |
| `shared` | `ui` | `BlockBoundary` |
| `@repo/ui` | `radarChart` | **`RadarChart` 표시 컴포넌트** — 축 · 값 · 상한을 props로 받는다. F006 FE가 만든다(FE-REQ-005 토큰 갭, 첫 소비처 B13) *(2026-09-21)* |

## 3탭 IA

| 탭 | 경로 | 내용 |
|---|---|---|
| 홈 | `/` | `home-briefing` 3블록 + 기존 블록 (읽기 전용) |
| 코치 | `/coach` | `coach-console` — 대화가 중심 |
| 자산 | `/assets` · **`/investments`** · `/assets/watchlist` | `포지션` · `시장`(기존 경로 유지) · `관심 종목` *(개정 2026-09-21, D7)* |
| (push) | `/alerts` · `/settings` · `/assets/transactions` · 상세(F004) | 탭 아님 *(2026-09-21)* |

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | **탭이 정확히 3개**다. 4개로 늘리지 않는다 | Must |
| FR-2 | 자산 탭 세그먼트 3개: **`포지션` / `시장` / `관심 종목`**. 기본은 `포지션`. 세그먼트는 경로이고 전환은 같은 zone soft navigation. **개정 2026-09-21** (D7 · ADR-002) | Must |
| FR-3 | **`/tax` 진입 링크를 두지 않는다**(홈 `CrossZoneLink` 포함). `apps/web-tax` zone을 걷을지는 열린 질문 Q1 — 이 REQ는 코드만 남기고 링크를 뺀다. **개정 2026-09-21** (ADR-002) | Must |
| FR-4 | 탭바에 `role="tablist"` + `aria-current`. 터치 타깃 44×44 | Must |
| FR-5 | 어떤 블록을 눌러도 해당 탭으로 이동한다. **홈에서 끝나는 액션이 없다** | Must |

## 홈 3블록

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-10 | **순서 고정**: ① 총자산 ② 이번 주 적립 ③ AI 추천. 그 아래 기존 목표 카드 · 투자 요약(`AnalysisGraph`) · `TipsApp`을 **지금 순서 · 디자인 그대로**. **설정 없음**. **개정 2026-09-21** (D6 — FEATURE-000 FR-30 대체) | Must |
| FR-11 | **375×667에서 스크롤 전 ①②③이 보인다** | Must |
| FR-12 | 블록마다 `BlockBoundary`(Suspense + error boundary) | Must |
| FR-13 | 블록 하나 실패 시 **그 블록만** "지금 불러올 수 없습니다" + 재시도 | Must |
| FR-14 | **금액 블록 실패 시 `0`을 표시하지 않는다** | Must |
| FR-15 | 비원화 환산이 있으면 총자산에 **환율 기준 툴팁**: "현재 환율 기준 환산". 전부 원화면 툴팁 없음. **개정 2026-09-21** — 세금 비교 문구 삭제(ADR-002) | Must |
| FR-16 | AI 추천 블록에 **F004 3종 세트 게이트**가 적용된다. 미충족이면 "표시할 추천이 없습니다" + 사유. 신뢰도 % 없음(D3) | Must |
| FR-17 | **홈은 읽기 전용.** 적립 완료 체크·피드백 같은 mutation을 두지 않는다 | Must |
| FR-18 | **홈에 알림 목록 · 보유 목록 블록을 두지 않는다.** 알림은 헤더 벨(FR-81), 보유는 `포지션`. **개정 2026-09-21** (D6 · D5) | Must |
| FR-19 | 온보딩 미완료면 **온보딩 카드 1개**만. 블록마다 중복 안내 0건 | Must |
| FR-20 | 스켈레톤 높이 = 실제 블록 높이 | Must |
| FR-21 | **변경 금지 목록**: 목표 진행 카드·`AnalysisGraph`·`TipsApp`은 **홈 3블록 아래 지금 자리**다. 삭제 · 이동 0건. **개정 2026-09-21** (D6) | Must |
| FR-22 | `TipsApp` **개인화** — 보유 · 적립 상태로 팁을 고른다. 문구는 금지 표현 검수. **(기본안 — 감사 문서 B6)** | Could |
| FR-23 | **PC 홈 최대 폭**(예: 960px)과 가운데 정렬. 세로 스택 유지. **(기본안 — 감사 문서 B7)** | Should |
| FR-24 | 블록 경계는 3블록 + 기존 2(목표 · 투자 요약) = **5개**다. `TipsApp`은 정적이라 경계가 없다(FE-REQ-008 FR-26 상한 5) | Must |

## 코치 대화 — 제품의 중심

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-30 | `ChatBubble` + `TextArea autoResize` + `Chip`(추천 질문) + `Spinner` | Must |
| FR-31 | **첫 메시지는 오늘의 브리핑 카드**다. 빈 화면으로 시작하지 않는다 — 무엇을 물어야 할지 모르는 것이 대화형 UI의 최대 이탈 원인이다 | Must |
| FR-32 | **추천 질문 Chip 4개**: "지금 팔아야 하나?" · "이번 주 얼마 넣을까?" · "내 포트폴리오 위험한가?" · "내가 뭘 잘못했나?". **개정 2026-09-21** — 세금 질문 교체(ADR-002, FEATURE-006 FR-22) | Must |
| FR-33 | 답변이 **토큰 단위로 흐른다**(SSE). 스트리밍 중 `ChatBubble streaming` + 점 세 개 | Must |
| FR-34 | 대화 목록에 **`role="log"`** 를 둔다 | Must |
| FR-35 | 답변 안에 **추천 카드**가 붙는다. F004의 `RecommendationCard`를 **재사용**한다 | Must |
| FR-36 | **카드에 3종 세트 게이트가 적용**된다. 미충족이면 카드 없이 텍스트만 | Must |
| FR-37 | LLM 실패 시 `fallbackText` + **`규칙 기반 설명` 배지** | Must |
| FR-38 | **AI 생성물을 `colors.ai.*` + `Badge tone="ai"`** 로 표시하고 생성 시각을 붙인다 | Must |
| FR-39 | 대화 히스토리를 **역순 페이징**으로 위로 불러온다 | Must |
| FR-40 | 전송 실패 시 **입력을 보존**하고 재시도 버튼 | Must |
| FR-41 | 스트리밍 중 **취소 버튼** | Must |
| FR-42 | **면책 문구가 답변에 항상 붙는다** | Must |
| FR-43 | 입력창은 `TextArea autoResize` + `maxHeight`. 긴 질문도 쓸 수 있다 | Must |
| FR-44 | 전송은 Enter(Shift+Enter 줄바꿈). 모바일 웹에서는 버튼 | Should |

## PC 이동식 격자

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-50 | PC(≥1280px)에서 `MovableGrid`로 패널을 동시에 배치한다 | Must |
| FR-51 | 기본 배치: **좌 코치 대화 / 우상 추천 카드 / 우하 포지션 요약**. 격자는 **코치 탭**이다 — `자산 → 시장`은 지금 2컬럼 그대로이고 격자에 넣지 않는다(FR-91). **개정 2026-09-21** (ADR-002 · D7) | Must |
| FR-52 | 패널 머리말을 끌어 이동, 구분선을 끌어 크기 조절 | Must |
| FR-53 | 구분선에 `role="separator"` + **화살표 키 조절** | Must |
| FR-54 | **칸 이동이 포인터 전용이므로 `movePanel`을 메뉴로 노출**한다(키보드 대체 수단) | Must |
| FR-55 | 배치를 저장한다(`PUT /api/app/panel-layout`). **저장 실패해도 기본 배치로 동작**한다 | Should |
| FR-56 | `MovableGrid`는 **`ssr: false`** 로 마운트한다 | Must |
| FR-57 | 1280px 미만에서는 격자를 쓰지 않고 3탭으로 동작한다 | Must |
| FR-58 | 격자 안에 들어가는 것은 **위젯**이다. `pc-panel-grid`가 위젯을 import하면 cross-slice이므로 **`pages`가 children으로 주입**한다 | Must |

## 온보딩 3스텝 (2026-09-21 개정)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-60 | ① 초대 코드 ② **첫 보유 기록 입력**(건너뛰기 가능) ③ 월 적립액. **그 외 질문 없음**(F000 FE-REQ-010과 공유). 2단계는 `record-transaction` 시트를 그대로 쓰고, 3단계는 F003 적립 설정에 **실제로 저장**한다. `OnboardingStepKey`의 `link_account` → `first_holding`(**BREAKING**). **개정 2026-09-21** (ADR-002 · **기본안 — 감사 문서 B12**) | Must |
| FR-61 | 온보딩 미완료면 홈 대신 온보딩 카드 | Must |

## 공통 헤더 · 진입점 (2026-09-21 추가)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-80 | **3탭 공통 헤더**: 좌 프로필(→ `/settings`) · 우 `검색` · `알림 벨`. 홈의 `ProfileHeader`를 이 헤더로 흡수한다(시각은 유지) | Must |
| FR-81 | 벨에 **안 읽은 수 배지**(0이면 숨김, 99+). `aria-label="알림 N건 안 읽음"` (D5) | Must |
| FR-82 | `검색` → 종목 검색(F000, D8). 결과 행 → 상세 분석 페이지 | Must |

## 자산 탭 — 시장 · 관심 종목 (2026-09-21 추가, D7)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-90 | `시장` = 지금 `pages/investments` + `widgets/market-board`. **경로 `/investments`를 유지**하고 세그먼트 헤더만 위에 얹는다 | Must |
| FR-91 | PC(≥1280px)는 **지금 2컬럼 그대로**(실시간 표 + 우측 AI 코치 패널). `MovableGrid`에 넣지 않는다. 우측 패널의 내용은 **F004 FE REQ**가 정한다 | Must |
| FR-92 | 1280px 미만은 **표 1열**. 행을 누르면 상세 분석 페이지로 이동(패널 없음) | Must |
| FR-93 | `관심 종목` 표에 **신호 컬럼 없음**(D4). 추적 상한 10 · 추가는 검색(D8) — F000 | Must |

## 자산 탭 — 포지션 (2026-09-21 추가, B13 · B14)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-100 | **Hero**: 총 평가금 · 평가 손익(금액 · % · 부호 문자) · 투자 원금 · 보유 수 · 최대 비중 · 30일 수익률 + [거래 기록 추가]. PC는 가로 1줄, 모바일 웹은 2×3 **(기본안 — 감사 문서 B13)** | Must |
| FR-101 | **평가금 흐름**: `SegmentedControl`(1주/1개월/3개월/1년) + 선 차트 + **기간 최대 낙폭** 숫자. "과거 기준" 라벨. 예측선 · 목표선 0건 | Must |
| FR-102 | **보유 목록**: 종목 · 비중 · 평가금 · 손익률. 행 → 상세 분석 페이지. 세금 배지 0건 | Must |
| FR-103 | **리스크 레이더** 4축(집중도 · 변동성 · 낙폭 · 뉴스 리스크) — `RadarChart` + 축별 값/상한 표. 상한 초과 축은 `colors.status.danger` + 사유 문구. `value: null` 축은 "데이터 부족" | Must |
| FR-104 | **[거래 기록 추가] 바텀시트**(PC는 모달): 자산군 → 종목(검색) → 매수/매도 → 수량 → 단가 → 거래일 → **미리보기 카드**(보유 수량 · 평단 전 → 후, 총 취득금액) → 저장. 미리보기는 서버 값이다 **(기본안 — 감사 문서 B14)** | Must |
| FR-105 | 과매도는 저장 버튼 비활성 + 서버 사유 문구. 입력은 보존 | Must |
| FR-106 | **거래 목록**(`/assets/transactions`): 최신순. 행 → 같은 시트로 수정(종목 · 매수/매도는 잠김), 삭제는 `useDialog().confirm` | Must |
| FR-107 | 보유 0건: Hero 대신 첫 기록 안내 + [거래 기록 추가]. 차트 · 레이더를 그리지 않는다 | Must |
| FR-108 | 시트 · 목록에 외부 주문 링크 · 버튼 0건 | Must |
| FR-109 | 포지션 하단에 `보유 기록 관리 →`(거래 목록) · `설정 →` 링크 | Must |

## 알림 화면 (2026-09-21 추가, D5)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-110 | `/alerts`: 최신순 목록. 행 = 아이콘 · 제목(i18n) · "무엇을 확인하라" · 시각. 안 읽음은 굵게 + 점 | Must |
| FR-111 | 행을 누르면 **읽음 처리 + 딥링크**(`target` → 코치 탭 · 홈 적립 블록) | Must |
| FR-112 | 상단 [모두 읽음]. 안 읽은 것이 없으면 비활성 | Must |
| FR-113 | **두지 않는 것**: [+ 알림] · 조건 알림 · 알림 만들기 시트 · 투자 피드 · 북마크 섹션(북마크는 F000 뉴스) (D5 · **기본안 — 감사 문서 B4 · B19**) | Must |

## 설정 화면 (2026-09-21 추가, D9 · B16)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-120 | `/settings` 한 화면. 그룹 순서: ① 적립 기본액 ② 밴드 임계값 ③ 코치 ④ 알림 ⑤ 보유 기록 관리. PC는 2컬럼(①②③ / ④⑤), 그 외 세로 스택 | Must |
| FR-121 | ① 월 금액 입력 + **서버가 환산한 주간 기본액** 표시. 저장은 F003 경로 | Must |
| FR-122 | ② 밴드 임계값은 **읽기 전용 표** + "과거 적중률이 기본 밴드 기준이라 바꾸지 않습니다" (D9) | Must |
| FR-123 | ③ 코치 성향 · 기본 모드(단타/장기) · 알림 수준 — 편집 컴포넌트는 F004 FE REQ 소유, 이 화면은 자리를 준다 **(기본안 — 감사 문서 B16)** | Must |
| FR-124 | ④ 알림 켜기/끄기 `Switch` 1개 | Must |
| FR-125 | ⑤ `보유 기록 관리 →` 거래 목록 | Must |
| FR-126 | 그룹별 독립 실패 — 한 그룹 "불러올 수 없습니다"여도 나머지는 편집 가능 | Must |
| FR-127 | 계좌 연결 · 세금 파라미터 · 보류 기능 토글 0건 (ADR-002 · **기본안 — 감사 문서 B19**) | Must |

## UX 상태

- **Loading**: 블록별 스켈레톤. 총자산은 마지막 캐시를 회색으로 먼저. 포지션은 섹션별(Hero · 차트 · 목록 · 레이더) 스켈레톤
- **Empty (보유 0건)**: FR-107. **Empty (알림 없음)**: "새 알림이 없습니다"
- **Empty (온보딩 미완료)**: 온보딩 카드 1개
- **Empty (대화 없음)**: FR-31 브리핑 카드 + FR-32 Chip. **빈 입력창만 0건**
- **Partial**: 블록별 "지금 불러올 수 없습니다" + 재시도
- **Blocked (게이트)**: "표시할 추천이 없습니다" + 사유
- **Degraded (LLM 실패)**: `fallbackText` + 배지
- **Error (전체 실패)**: 마지막 성공 응답 + 갱신 실패 배너
- **Optimistic update**: **없음**(홈은 읽기 전용, 대화는 서버 응답이 내용)

## 접근성

| ID | 요구사항 |
|---|---|
| FR-70 | 탭바 `role="tablist"` + `aria-current`. 터치 타깃 44×44 |
| FR-71 | 총자산 큰 숫자에 `aria-label` |
| FR-72 | 손익 부호를 색 + `+`/`−` 문자 |
| FR-73 | 대화 목록에 `role="log"`. **스트리밍 중 매 토큰 낭독하지 않는다** — 완료 시에만 알린다 |
| FR-74 | 스트리밍 중 `aria-busy` |
| FR-75 | 구분선 `role="separator"` + 화살표 키 |
| FR-76 | 격자 이동의 키보드 대체 수단(FR-54) |
| FR-77 | 늦게 오는 블록에 `aria-busy` |

## Acceptance Criteria

- [ ] **탭이 정확히 3개다**
- [ ] 자산 탭 세그먼트가 `포지션` / `시장` / `관심 종목`이다
- [ ] **`/tax` 링크 · 세금 칩 · 청구서 문구가 화면 코드에 0건이다**
- [ ] 탭바에 `role="tablist"` + `aria-current`가 있다
- [ ] **홈 블록 순서가 고정이고 설정 항목이 0건이다**
- [ ] **375×667 스크롤 전에 총자산·적립·추천이 보인다**
- [ ] 블록 하나 실패 시 그것만 에러이고 나머지 2개와 기존 블록이 정상이다
- [ ] 금액 블록 실패 시 `0`이 0건이다
- [ ] 총자산에 환율 기준 툴팁이 있다
- [ ] **AI 추천 블록에 게이트가 적용된다**
- [ ] **홈에 mutation 호출이 0건이다**
- [ ] 온보딩 미완료 시 카드 1개만 나온다
- [ ] 스켈레톤 높이가 실제와 같다 (CLS 측정)
- [ ] **`TipsApp`·목표 카드·`AnalysisGraph`가 홈 3블록 아래 지금 순서 · 디자인 그대로다**
- [ ] 대화 첫 진입에 브리핑 카드 + Chip 4개가 있다 (**빈 입력창만 0건**)
- [ ] 답변이 토큰 단위로 흐르고 `streaming` 표시가 있다
- [ ] 대화 목록에 `role="log"`가 있다
- [ ] 답변 안 추천 카드가 F004 컴포넌트를 재사용한다
- [ ] **카드에 게이트가 적용되고 미충족 시 텍스트만 나온다**
- [ ] LLM 실패 시 `fallbackText` + 배지가 나온다
- [ ] AI 생성물이 `colors.ai.*` + `Badge tone="ai"` + 생성 시각으로 구분된다
- [ ] 히스토리가 역순 페이징으로 불러와진다
- [ ] 전송 실패 시 입력이 보존된다
- [ ] 스트리밍 중 취소 버튼이 있다
- [ ] **면책이 답변에 항상 붙는다**
- [ ] PC에서 `MovableGrid`로 패널을 이동·조절할 수 있다
- [ ] 구분선이 `role="separator"`이고 화살표 키로 조절된다
- [ ] **격자 이동의 키보드 대체 수단이 있다**
- [ ] 배치 저장 실패 시 기본 배치로 동작한다
- [ ] `MovableGrid`가 `ssr: false`다
- [ ] 1280px 미만에서 3탭으로 동작한다
- [ ] 스트리밍 중 스크린리더가 매 토큰 낭독하지 않는다
- [ ] 375px에서 body 가로 스크롤이 0이다
- [ ] 공통 헤더에 프로필(→ 설정) · 검색 · 벨이 있고 벨 배지가 안 읽은 수와 같다
- [ ] `시장`이 `/investments` 경로이고 PC에서 지금 2컬럼 그대로다 (격자 0건)
- [ ] `관심 종목`에 신호 컬럼이 0건이다
- [ ] 포지션 Hero 6항목 · 평가금 흐름 4기간 + MDD · 보유 목록 · 레이더 4축이 있다
- [ ] 거래 시트 미리보기가 서버 값이고 과매도 시 저장이 막힌다
- [ ] 거래 수정 · 삭제가 동작하고 삭제에 confirm이 뜬다
- [ ] 보유 0건에서 0원 차트 · 레이더가 그려지지 않는다
- [ ] `RadarChart`가 `@repo/ui`에 있고 축 · 값 · 상한을 props로 받는다
- [ ] 알림 화면에 읽음 · 모두 읽음이 있고 [+ 알림] · 피드가 0건이다
- [ ] 설정이 한 화면 5그룹이고 밴드 임계값이 읽기 전용이다
- [ ] 설정에 계좌 연결 · 세금 파라미터 · 보류 토글이 0건이다
- [ ] 온보딩 2단계가 거래 시트이고 건너뛸 수 있다. 3단계가 F003에 저장된다
- [ ] PC 홈에 최대 폭이 있다 (Should)

## Dependencies

- **선행:** `FE-REQ-007`(zone) · `008`(스트리밍) · `009`(FSD) · `026`(F004 카드) · `BFF-REQ-028`
- **선행:** `feature/repo-ui-component` 병합 (`ChatBubble`·`TextArea autoResize`·`MovableGrid`·`Chip`·`Spinner`)
- **짝:** `FE-REQ-031`(FUNC) · `032`(API) · `033`(PERF)
- **규칙:** `fsd-pages.md` · `fsd-widgets.md` · `a11y-policy.md`

## Open Questions

- ~~변경 금지 목록을 3탭 어디에 둘지~~ → **2026-09-21 해소(D6).** 홈 3블록 아래 지금 자리.
- ~~자산 탭 세그먼트를 같은 화면의 필터로~~ → **2026-09-21 개정.** `시장`은 WS 화면이라 한 번에 받을 수 없다. 세그먼트 = 경로(FR-2).
- PC 격자 기본 배치와 저장 여부. 사용자가 바꾼 배치를 저장하면 기기별로 달라진다.
- 대화 입력의 Enter 전송이 긴 질문 작성을 방해할 수 있다.
- **Q1 — `apps/web-tax` zone.** 링크를 빼면 zone은 도달 경로가 없는 코드가 된다. 걷을지 다른 경계로 쓸지는 결정 전(ADR-002 §5).
- **Q2 — 자산군.** 거래 시트 첫 단계(자산군)는 자산군 3종 유지 여부에 달려 있다. 크립토 하나면 단계를 없앤다.
- 공통 헤더가 홈 `ProfileHeader`의 시각을 바꾸는가. "홈 변경 금지"(FEATURE-000)와 부딪히지 않게 **프로필 영역 시각은 그대로 두고 우측에 아이콘 두 개만** 더하는 것이 기본안.

## Changelog

| 날짜 | 변경 |
|---|---|
| 2026-09-21 | ADR-002 머리 배너. 개정: FSD 표 · 3탭 경로 · FR-2(세그먼트 D7) · FR-3(`/tax` 링크 제거, Q1) · FR-10(3블록 D6) · FR-15 · FR-16 · FR-18 · FR-21 · FR-32(Chip) · FR-51(격자 = 코치 탭) · FR-60(온보딩 B12, **BREAKING** 단계 키). 추가: FR-22(B6) · FR-23(B7) · FR-24 · FR-80~82(헤더 · 검색 D8) · FR-90~93(시장 · 관심 종목 D4 · D7) · FR-100~109(포지션 · 거래 시트 · `RadarChart`, B13 · B14) · FR-110~113(알림 D5 · B4 · B19) · FR-120~127(설정 D9 · B16 · B19). 근거: `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` · `ADR-002` |
