---
id: FE-REQ-027
feature: F004
area: fe
kind: FUNC
title: "F004 AI 코치 추천 — 웹 기능 규칙 정의 (게이트 강제 · 문구 검수 · 쿨다운)"
priority: critical
labels: [fe, func, fsd, render-gate, i18n-gate, cooldown]
created: 2026-09-09
---

## Summary

F004의 기능 규칙은 거의 전부 **"하지 않는 것"** 이다. 게이트를 우회하지 않고, 금액·점수를 계산하지 않고, 확신 표현을 쓰지 않고, 주문을 실행하지 않는다.

## 렌더 게이트 — 컴포넌트가 강제한다

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | 게이트 판정은 **서버가 준 `renderable`을 그대로 쓴다.** 프론트가 다시 판정하지 않는다 | Must |
| FR-2 | 다만 **방어적으로 재확인**한다: `renderable === true`인데 `signalTrackRecord`가 null이거나 `failureCases`가 비면 **렌더하지 않고 개발 로그에 남긴다.** 계약 위반이다 | Must |
| FR-3 | 게이트 로직을 `entities/coach/lib/renderGate.ts`에 순수 함수로 둔다 | Must |
| FR-4 | **우회 prop·플래그·환경변수를 만들지 않는다** | Must |
| FR-5 | `blockedReason` 3종을 i18n 키로 매핑한다. 매핑 없으면 일반 문구로 폴백하되 개발 로그에 남긴다 | Must |
| FR-6 | `disclaimer`가 없으면 **화면 전체를 렌더하지 않는다** | Must |
| FR-7 | `scoreNote`가 없으면 **점수를 렌더하지 않는다** | Must |

## 계산하지 않는다

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-10 | **점수를 계산하지 않는다.** 서버 값 표시만 | Must |
| FR-11 | **승률·평균수익·MDD를 계산하지 않는다.** 서버 값 표시만 | Must |
| FR-12 | **`priceGap`을 계산하지 않는다.** 서버가 준다. 받은 가격으로 % 를 만들지도 않는다(D13) | Must |
| FR-13 | **preflight 결과를 계산하지 않는다.** 입력을 서버에 보내고 결과를 받는다 | Must |
| FR-14 | 표시 포맷(퍼센트·부호·통화)만 한다 | Must |
| FR-15 | `staleHours`는 **서버 값을 쓴다.** 클라이언트 시계로 재계산하면 시계가 틀린 기기에서 배지가 잘못 뜬다 | Must |

## 문구 검수 — 이 기능의 최대 위험

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-20 | 사용자 문구는 전부 `shared/i18n`에 있다 | Must |
| FR-21 | **금지 표현 검사**: "확실" · "무조건" · "보장" · "100%" · "목표주가" · "예상 수익률" · "당신은" · "패닉셀러" · "실수" · "잘못" · "점수가 높으니" | Must |
| FR-22 | 서버 `factCode`·`conditionCode`·`reasonCode`·`blockedReason`을 i18n 키로 매핑한다 | Must |
| FR-23 | **매핑 없는 코드를 원문으로 노출하지 않는다** | Must |
| FR-24 | 행동 기록 문구를 **사실 서술로만** 만든다. 인격 평가 0건 | Must |
| FR-25 | LLM 해설(`explanation.text`)은 **서버가 후처리한 것**이다. 프론트가 다시 검사하지 않되, **개발 환경에서 금지 표현 스캔 경고**를 낸다 | Should |
| FR-26 | 익절 가격 문구에 "목표주가"·"예상"을 쓰지 않는다. **"내 규칙 기반 가격"** 임을 명시 | Must |

## 주문을 실행하지 않는다

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-30 | **"주문하기"·"매수하기"·"매도하기" 버튼을 만들지 않는다** | Must |
| FR-31 | preflight 결과 끝에 **"업비트/증권사에서 직접 주문하세요"** 를 표시한다 | Must |
| FR-32 | 거래소 딥링크·외부 링크를 **만들지 않는다.** 편의로 보이지만 주문 유도로 읽힐 수 있다 | Must |
| FR-33 | preflight에 **차단·게이트 로직을 넣지 않는다.** 상한 초과여도 결과를 보여준다 | Must |

## 쿨다운

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-40 | 재생성 쿨다운은 **서버가 판정**한다. 프론트가 타이머를 신뢰하지 않는다 | Must |
| FR-41 | `generation-status`의 `retryAfterSeconds`로 버튼 상태와 남은 시간을 표시한다 | Must |
| FR-42 | **로컬 타이머로 카운트다운**하되, 0이 되면 `generation-status`를 재조회해 확인한다 | Must |
| FR-43 | 429를 받으면 `retryAfterSeconds`로 타이머를 갱신한다. 오류 토스트를 띄우지 않는다 | Must |
| FR-44 | 재생성 mutation은 **재시도하지 않는다** | Must |
| FR-45 | `generate`가 202를 주면 **완료를 폴링**한다. `generation-status` 또는 추천 재조회 | Must |
| FR-46 | 폴링에 **중단 조건**: 완료 · 실패 · 언마운트 · 최대 횟수(15회 = 30초) | Must |

## 피드백

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-50 | `[도움 안 됨]`을 누르면 **사유 선택**을 띄운다(4종). 사유 없이 보내지 않는다 | Must |
| FR-51 | `[도움 됐음]`은 사유 없이 즉시 전송 | Must |
| FR-52 | **낙관적 갱신을 허용**한다(금액이 아니다). 실패 시 되돌리고 토스트 | Must |
| FR-53 | mutation 재시도 0회 | Must |
| FR-54 | 이미 남긴 피드백이 있으면 그 상태를 표시하고 **변경 가능**하게 한다(upsert) | Must |

## 상태 관리

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-60 | 조회는 **서버 컴포넌트**. React Query는 mutation·폴링·preflight에만 | Must |
| FR-61 | preflight는 **입력마다 부르지 않는다.** 디바운스 500ms 또는 `[계산]` 버튼 | Must |
| FR-62 | 성향 설정 저장 후 **라우트 재검증**(`router.refresh()`). 성향이 점수에 영향한다 | Must |
| FR-63 | 낙관적 갱신은 **피드백만** | Must |
| FR-64 | 선택된 종목·모드를 URL 쿼리로 유지한다. **개정 2026-09-21 (D3)**: Should → **Must**. 모드는 패널과 상세 분석 페이지가 공유하는 상태이고 URL 이 유일한 저장소다(FR-81) | Must |

## 종목 판단 — 패널 · 상세 분석 페이지 규칙 (2026-09-21)

근거: `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` D2 · D3 · D4 · B1 · B3 · B9 · B10 · B16.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-80 | 종목 판단 게이트는 **모드별**이다. `modes[mode].renderable` 을 그대로 쓰고, 방어적 재확인(FR-2)을 모드마다 한다. 게이트 함수는 FR-3 의 `renderGate.ts` 하나 | Must |
| FR-81 | **모드 상태의 유일한 저장소는 URL `?mode=`** 다. 전역 store · `localStorage` 에 따로 두지 않는다. URL 에 없으면 응답의 `mode`(서버가 `defaultMode` 반영 — B16). 프론트가 `scalp` 를 기본값으로 박지 않는다 | Must |
| FR-82 | 모드 스위치는 `defaultMode` 를 **저장하지 않는다.** 저장은 성향 설정(FR-80~81 of FE-REQ-026)에서만 | Must |
| FR-83 | **신뢰도를 그리지 않는다(D3).** 뷰모델에 없으므로 코드도 없어야 한다. 점수를 퍼센트 기호와 함께 쓰지 않는다("64점", "64%" ❌) | Must |
| FR-84 | 유효시간은 `validity.code` → i18n 매핑 하나다. **화면이 시간을 계산하거나 "25분"·"30일" 같은 상수를 두지 않는다** | Must |
| FR-85 | 구간(`zone`) · 오버레이 선 · 현재가와의 거리 · 게이지 적중률을 **계산하지 않는다.** 서버 값 표시와 포맷만(FR-10~14 확장) | Must |
| FR-86 | 금지 표현(FR-21)에 **"매수존" · "매수 적정가" · "목표가" · "상승 여력" · "신뢰도" · "기대 수익"** 을 더한다. 구간 · 해설 · 적중률 문구가 검사 대상이다 | Must |
| FR-87 | `예측 아님` 배지는 `zone.notPrediction` 과 게이지 적중률 줄에 **항상** 붙는다. prop 으로 끌 수 없다 | Must |
| FR-88 | 해설 `[해설 보기]` 는 `modes[mode].renderable === true` 일 때만 렌더한다. 해설 응답이 `{ renderable: false }` 면 버튼 자리에 `BlockedNotice` 문구 | Must |
| FR-89 | 해설 `newsSummary` 는 최대 5줄만 그린다(서버가 지키지만 넘치면 자른다 — 레이아웃 방어) | Should |
| FR-90 | 주문 전 체크: 목표가 입력의 초기값은 **빈 문자열**이다. `preflightDefaults` 에 목표가가 없고, 구간 가격을 목표가에 채워 넣는 코드 0건(B1) | Must |
| FR-91 | 손절 칩은 `stopLossRate` 를 보낸다. 프론트가 `entryPrice × (1 + rate)` 를 계산하지 않는다 | Must |
| FR-92 | "전액" 칩 · 잔여 현금 표시 0건(현금 기록 없음 — B13) | Must |
| FR-93 | **관심 종목 표가 코치 뷰모델을 읽지 않는다**(D4). 관심 종목 행에 판단 · 신호를 붙이는 코드 0건 | Must |
| FR-94 | 코치 리포트 전부 미렌더(B18)를 **정상 상태**로 분기한다: 에러 경계 · 오류 토스트로 보내지 않는다 | Must |
| FR-95 | 추천 근거 상세의 적중 · 실패 이력은 **같은 컴포넌트**(`TrackCaseList`)를 `tone` 만 달리해 그린다 — 구현이 같아야 비중이 같다(B2) | Must |

## 부분 실패

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-70 | `degradedFields[]`에 있는 필드는 그 블록만 "불러올 수 없습니다" | Must |
| FR-71 | **추천이 `null`이면 "아직 추천이 없습니다" + [생성]** 을 보여준다 | Must |
| FR-72 | 성적표·실패이력 실패는 **게이트로 이어진다.** 그건 정상 동작이므로 에러로 표시하지 않는다 | Must |
| FR-73 | `explanation`이 없으면 해설 영역을 렌더하지 않는다 | Must |

## Acceptance Criteria

- [ ] 게이트 판정이 서버 `renderable`을 쓴다
- [ ] **`renderable: true`인데 3종 중 하나가 없으면 렌더하지 않고 개발 로그에 남긴다**
- [ ] 게이트가 순수 함수로 있다
- [ ] **우회 prop·플래그·환경변수가 0건이다**
- [ ] `blockedReason` 3종이 i18n 키로 매핑된다
- [ ] `disclaimer` 없으면 화면이 렌더되지 않는다
- [ ] `scoreNote` 없으면 점수가 렌더되지 않는다
- [ ] **프론트에 점수·승률·MDD·`priceGap`·preflight 계산 코드가 0건이다**
- [ ] `staleHours`가 서버 값이다 (클라이언트 재계산 0건)
- [ ] 사용자 문구가 전부 `shared/i18n`에 있다
- [ ] **금지 표현 검사가 동작한다** (11개 표현)
- [ ] 서버 코드 4종이 i18n 키로 매핑된다
- [ ] 매핑 없는 코드가 원문으로 노출되지 않는다
- [ ] 행동 기록에 인격 평가 문구가 0건이다
- [ ] 개발 환경에서 LLM 해설 금지 표현 스캔 경고가 나온다
- [ ] 익절 문구에 "목표주가"·"예상"이 0건이다
- [ ] **"주문하기"·"매수하기"·"매도하기" 버튼이 0건이다**
- [ ] preflight 끝에 "직접 주문하세요"가 있다
- [ ] **거래소 딥링크·외부 링크가 0건이다**
- [ ] preflight에 차단 로직이 0건이다
- [ ] 쿨다운이 서버 판정이고 로컬 타이머가 0이 되면 재조회한다
- [ ] 429가 오류 토스트가 아니라 타이머 갱신이다
- [ ] 재생성 mutation 재시도가 0건이다
- [ ] `generate` 202 후 폴링이 동작하고 **중단 조건 4개가 있다**
- [ ] `[도움 안 됨]`이 사유 선택을 띄우고 사유 없이 전송되지 않는다
- [ ] 피드백이 낙관적 갱신이고 실패 시 되돌아간다
- [ ] 이미 남긴 피드백을 변경할 수 있다
- [ ] 조회가 서버 컴포넌트에서 일어난다
- [ ] preflight가 디바운스되거나 버튼으로 실행된다
- [ ] 성향 저장 후 라우트가 재검증된다
- [ ] 낙관적 갱신이 피드백만이다
- [ ] `degradedFields` 블록만 에러 상태다
- [ ] 성적표·실패이력 실패가 에러로 표시되지 않는다
- [ ] 종목 판단 게이트가 모드별이고 모드마다 방어적 재확인이 있다
- [ ] **모드가 URL `?mode=` 에만 있다** (store · `localStorage` 0건) — 패널 → 상세 이동 후에도 같다
- [ ] 프론트 코드에 `scalp` 하드코딩 기본값이 0건이다
- [ ] 모드 스위치가 프로필을 저장하지 않는다
- [ ] **신뢰도 · 점수 `%` 표기가 0건이다**
- [ ] 유효시간 상수("25분"·"30일")가 0건이다
- [ ] 구간 · 거리 · 게이지 적중률 계산 코드가 0건이다
- [ ] 금지 표현 검사에 6개가 추가되어 17개다
- [ ] `예측 아님` 배지를 끄는 prop 이 0건이다
- [ ] 판단 미렌더 모드에서 [해설 보기] 가 렌더되지 않는다
- [ ] 목표가 초기값이 빈칸이고 구간 가격을 채우는 코드가 0건이다
- [ ] 손절 칩이 `stopLossRate` 를 보낸다
- [ ] "전액" 칩 · 잔여 현금이 0건이다
- [ ] 관심 종목 표에 판단 · 신호가 0건이다
- [ ] 리포트 전부 미렌더가 에러 경계로 가지 않는다
- [ ] 적중 · 실패 이력이 같은 컴포넌트다

## Dependencies

- **선행:** `FE-REQ-026`(UI) · `FE-REQ-009`(FSD) · `BFF-REQ-024`
- **규칙:** `i18n-policy.md` · `state-convention.md` · `fsd-features.md`

## Open Questions

- FR-2(방어적 재확인)가 **서버를 불신하는 코드**다. 계약 테스트가 있으면 불필요할 수 있지만, **게이트 우회가 가장 위험한 실패**이므로 이중 방어가 정당하다고 본다.
- LLM 해설을 프론트에서 다시 검사할지(FR-25). **서버가 후처리하므로 중복**이지만 개발 중 발견 가치가 있다 → 개발 환경 경고만.
- preflight를 디바운스할지 버튼으로 할지. **버튼이 명확하다**(계산 의도를 사용자가 표현한다). 2026-09-21: 칩을 누를 때마다 계산할지도 같은 질문이다 — 버튼 유지가 기본안.
- 모드를 URL 에만 두면 투자 화면을 새로 열 때마다 `defaultMode` 로 돌아간다. **마지막 모드를 기억할지**는 D3 이 정하지 않았다 — 기억하지 않는 것이 기본안.

## Changelog

| 날짜 | 변경 |
|---|---|
| 2026-09-21 | `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` 반영. 신규 FR-80~95(모드별 게이트(B10) · 모드 URL 단일 저장소 · `defaultMode` 위임(B16) · 신뢰도/`%` 금지 · 유효시간 상수 금지(D3) · 구간/거리/적중률 무계산(D2 · B9) · 금지 표현 6개 추가 · `예측 아님` 강제 · 해설 게이트(B3) · 목표가 빈칸/손절 칩/전액 없음(B1) · 관심 종목 분리(D4) · 초기 전부 미렌더 정상(B18) · 적중/실패 같은 컴포넌트(B2)). 개정: FR-64 Should → Must |
| 2026-09-21 | `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` D11 ~ D13 반영. FR-12 개정(D13). `distancePct` → `priceGap` |
