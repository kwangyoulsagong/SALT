---
id: FE-REQ-011
feature: F000
area: fe
kind: FUNC
title: "F000 정리·편집 — 웹 기능 규칙 정의 (수리 동작 · 상태 동기화 · 문구 정리)"
priority: high
labels: [fe, func, fsd, repair, state-sync, i18n]
created: 2026-09-09
---

## Summary

수리 9건의 **동작 규칙**과 초대·온보딩의 상태 관리. 그리고 사용자 노출 문구를 `shared/i18n`으로 모으는 작업(현재 `constants/api.ts`에 섞여 있다).

## 상태 동기화 — 관심 종목

실시간 탭의 별 아이콘과 관심 종목 탭 목록이 **같은 상태를 봐야 한다.**

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | 관심 종목은 `entities/market/model`의 단일 소스다. 두 화면이 같은 쿼리를 구독한다 | Must |
| FR-2 | 추가/제거는 `features/toggle-watchlist`의 mutation이다. 성공 시 **관심 종목 쿼리를 무효화**한다 | Must |
| FR-3 | **낙관적 갱신을 허용한다.** 별 아이콘은 금액이 아니고 즉시 반응이 중요하다 | Must |
| FR-4 | 실패 시 낙관적 갱신을 되돌리고 토스트를 띄운다 | Must |
| FR-5 | 관심 종목 추가 상한이 있으면 서버가 판정한다. 프론트가 세지 않는다. **개정 2026-09-21** — 상한이 생겼다(추적 10, 보유 제외 — D8). `409 TRACKED_ASSET_LIMIT` 이면 낙관적 ★ 를 되돌리고(FR-4) 토스트 대신 **인라인 안내**(`FE-REQ-010` FR-75). 카운터는 응답 · 무효화 후 BFF `tracked.count` 로만 바뀐다 | Must |
| FR-6 | 검색 화면 · 시세 표 · 관심 종목 탭의 ★ 가 **같은 쿼리**를 본다. 검색 결과의 `isTracked` 는 mutation 성공 시 검색 쿼리도 무효화해 맞춘다 (D8, 2026-09-21) | Must |
| FR-7 | 보유 종목(`isHeld`)의 ★ 해제 mutation 을 **보내지 않는다.** 버튼이 비활성이다 (D8, 2026-09-21) | Must |

## 종목 검색 동작 (2026-09-21, D8)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-80 | 검색어 상태는 `features/search-asset` 의 로컬 상태 + URL `?q=` 동기화(뒤로가기 · 새로고침 복원). 전역 스토어에 두지 않는다 | Must |
| FR-81 | 입력 **디바운스 300ms**. 이전 요청은 `AbortSignal` 로 취소한다. 응답이 뒤바뀌어 늦은 결과가 이기지 않게 한다 | Must |
| FR-82 | 앞뒤 공백 제거 후 빈 문자열이면 요청하지 않고 최근 검색 · 추적 목록을 보여준다 | Must |
| FR-83 | 최근 검색은 **결과 행을 눌렀을 때만** 저장한다(타이핑마다 저장하지 않는다). `localStorage` 접근은 try/catch — 실패해도 화면은 동작 | Could |
| FR-84 | 검색은 클라이언트 컴포넌트다. 서버 컴포넌트에서 사용자별 검색을 미리 렌더하지 않는다(`isTracked` 가 캐시에 섞이지 않게) | Must |

## 뉴스 북마크 동작 (2026-09-21, D5)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-85 | ☆ 는 `features/toggle-news-bookmark` 의 mutation. **낙관적 갱신 허용**(금액이 아니다). 성공 시 뉴스 쿼리 · 북마크 목록 쿼리를 무효화한다 | Must |
| FR-86 | 같은 기사가 프리뷰 · 북마크 목록 두 곳에 보여도 ☆ 상태가 같다 — 기사 id 기준 단일 소스 | Must |
| FR-87 | 빠른 연타는 마지막 의도만 보낸다(토글 중 버튼 비활성). mutation **재시도 0회** | Must |
| FR-88 | 북마크는 알림 상태(안 읽은 수 · 배지)를 건드리지 않는다 | Must |

## 목표 수량 폼 (2026-09-21)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-90 | `금액` / `수량` 전환 시 반대쪽 입력값을 **보내지 않는다.** 서버는 택일을 422 로 검사한다 (기본안 — 감사 문서 B5) | Should |
| FR-91 | 수량 입력은 문자열로 받아 **그대로** 보낸다. `Number()` 변환으로 소수 자리를 잃지 않는다 (기본안 — 감사 문서 B5) | Should |
| FR-92 | 진행률 · 현재 수량을 프론트가 계산하지 않는다. 서버 값을 표시한다(공통 기준 ③) (기본안 — 감사 문서 B5) | Should |

## WebSocket 마지막 수신 시각

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-10 | WS 메시지 수신 시각을 `entities/market/model`에 저장한다 | Must |
| FR-11 | 헤더에 그 시각을 **상대 시간 또는 절대 시각**으로 표시한다 | Must |
| FR-12 | WS가 끊기면 **"연결 끊김"** 을 표시한다. 오래된 시각을 그대로 두면 사용자가 최신이라고 오해한다 | Must |
| FR-13 | 시각 갱신이 **테이블 전체를 리렌더하지 않는다.** 헤더만 갱신 | Must |
| FR-14 | 하드코딩 문자열 `"실시간 오늘 19:30 기준"`이 코드에 0건이다 | Must |

## 터치 선택

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-20 | 행 선택에 `onClick`을 **추가**한다. `onMouseEnter`는 **유지**한다(PC hover 동작을 없애지 않는다) | Must |
| FR-21 | 키보드 선택(`onKeyDown` Enter/Space)을 추가한다 | Must |
| FR-22 | 선택된 행을 시각적으로 표시한다. hover와 선택을 **구분**한다 | Must |
| FR-23 | 터치 기기에서 hover가 sticky가 되지 않게 한다(`@media (hover: hover)`) | Must |

## 반응형 동작

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-30 | 모바일에서 `MarketPreview`를 **아래로 접는다.** 조건은 CSS 미디어 쿼리이고 **JS 분기를 만들지 않는다**(SSR 불일치 위험) | Must |
| FR-31 | JS로 뷰포트를 판단해야 하면 **마운트 후**에만 한다. 서버 렌더 결과가 달라지면 하이드레이션이 깨진다 | Must |
| FR-32 | 표 스크롤 위치를 종목 선택 시 유지한다. 선택마다 맨 위로 튀지 않게 | Should |

## 문구 정리 — `shared/i18n`으로 모은다

현재 `constants/api.ts`에 `HTTP_ERROR_MESSAGE`·`TOAST_MESSAGES`·`ERROR_MESSAGE`가 섞여 있다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-40 | 사용자 노출 문구를 전부 `shared/i18n`으로 옮긴다 | Must |
| FR-41 | `constants/api.ts`에는 **URL·상수만** 남는다 | Must |
| FR-42 | 오타 `덜 썻어요` → `덜 썼어요`를 i18n 카탈로그에서 고친다 | Must |
| FR-43 | **금지 표현 검사**를 카탈로그에 적용한다: "확실"·"무조건"·"보장"·"100%"·"당신은"·"점수"·"등급" | Must |
| FR-44 | 서버 `reasonCode`·`messageCode`를 i18n 키로 매핑한다. **매핑 없는 코드는 렌더하지 않는다** | Must |

## 초대·온보딩 상태

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-50 | 온보딩 단계를 서버 `onboarding/status`가 결정한다. **프론트가 판단하지 않는다** | Must |
| FR-51 | 코드 입력 중 `invite/check`를 **디바운스**해서 부른다(300ms). 타이핑마다 부르지 않는다 | Must |
| FR-52 | `invite/accept`는 mutation. **재시도 0회** — 코드가 소진된다 | Must |
| FR-53 | 성공 시 토큰을 저장하고 홈으로 이동한다. 저장 위치는 **쿠키**(`FE-REQ-008` 전환 후) | Must |
| FR-54 | 실패 `reasonCode` 3종을 각각 다른 문구로. **상한 초과는 서버가 노출하지 않으므로** 일반 실패로 처리한다 | Must |
| FR-55 | 비밀번호 입력은 `type="password"` + `autoComplete="new-password"` | Must |
| FR-56 | 온보딩 중 이탈 후 재진입 시 **완료한 단계를 건너뛴다** | Must |

## 인증 축소

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-60 | 회원가입·비밀번호 변경·계정 삭제 **화면과 훅을 제거**한다 | Must |
| FR-61 | 그 경로로 직접 진입하면 `/`로 리다이렉트한다 | Must |
| FR-62 | 토큰 만료 시 refresh를 시도하고 실패하면 로그인으로 보낸다 | Must |
| FR-63 | 로그인 화면에 **초대 안내**를 넣는다: "초대 코드가 있으신가요?" | Must |

## 동면 경로 호출 제거

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-70 | 미션·포인트·업적·피드·대시보드·랭킹 API 호출을 **전부 제거**한다 | Must |
| FR-71 | 관련 훅·쿼리 키를 제거한다 | Must |
| FR-72 | **410 응답을 받는 코드가 0건이어야 한다.** BFF 410 로그로 검증한다 | Must |
| FR-73 | 그 화면·위젯이 있으면 제거한다. **단 변경 금지 목록 항목은 제거하지 않는다** | Must |

## Acceptance Criteria

- [ ] 관심 종목이 단일 소스이고 두 화면이 동기화된다
- [ ] 별 아이콘에 낙관적 갱신이 적용되고 실패 시 되돌아간다
- [ ] WS 마지막 수신 시각이 헤더에 표시된다
- [ ] WS 끊김이 표시된다
- [ ] 시각 갱신이 테이블 전체를 리렌더하지 않는다 (Profiler)
- [ ] **하드코딩 `"실시간 오늘 19:30 기준"`이 0건이다**
- [ ] `onClick`이 추가되고 `onMouseEnter`가 유지된다
- [ ] 키보드 선택이 동작한다
- [ ] hover와 선택이 시각적으로 구분된다
- [ ] 터치 기기에서 hover가 sticky가 되지 않는다
- [ ] `MarketPreview` 접힘이 CSS 미디어 쿼리로 처리된다 (JS 분기 0건)
- [ ] 하이드레이션 경고가 0건이다
- [ ] 사용자 문구가 전부 `shared/i18n`에 있다
- [ ] `constants/api.ts`에 사용자 문구가 0건이다
- [ ] **`덜 썻어요`가 0건이다**
- [ ] 금지 표현 검사가 동작한다
- [ ] 매핑 없는 서버 코드가 렌더되지 않는다
- [ ] 온보딩 단계가 서버 응답으로 결정된다
- [ ] `invite/check`가 디바운스된다 (타이핑 5회 → 요청 1회)
- [ ] `invite/accept`가 재시도되지 않는다
- [ ] 실패 `reasonCode` 3종이 각각 다른 문구다
- [ ] 온보딩 재진입 시 완료 단계를 건너뛴다
- [ ] 회원가입·비밀번호·계정삭제 화면·훅이 0건이다
- [ ] 그 경로 직접 진입이 `/`로 리다이렉트된다
- [ ] 로그인 화면에 초대 안내가 있다
- [ ] **동면 API 호출이 0건이다** (BFF 410 로그 1주 검증)
- [ ] 변경 금지 목록 항목이 제거되지 않았다

**추가 2026-09-21**

- [ ] 추적 상한 409 에서 ★ 가 되돌아가고 인라인 안내가 뜬다
- [ ] 검색 · 시세 표 · 관심 종목 탭의 ★ 상태가 한 번의 토글로 모두 맞는다
- [ ] 보유 종목 ★ 해제 요청이 0건이다
- [ ] 검색이 디바운스되고(타이핑 5회 → 요청 1회) 늦은 응답이 최신 결과를 덮지 않는다
- [ ] `?q=` 로 새로고침 시 검색이 복원된다
- [ ] ☆ 가 낙관적으로 바뀌고 실패 시 되돌아간다. 프리뷰 · 북마크 목록의 ☆ 가 같다
- [ ] 북마크가 알림 배지를 바꾸지 않는다
- [ ] 수량 목표 요청 본문에 금액 필드가 없고 수량 소수 자리가 보존된다

## Dependencies

- **선행:** `FE-REQ-010`(UI) · `FE-REQ-009`(FSD) · `BFF-REQ-008`
- **규칙:** `i18n-policy.md` · `state-convention.md` · `ssr.md`

## Open Questions

- 토큰 저장을 언제 쿠키로 옮길지(`FE-REQ-008` Open Question). F000에서 초대 화면을 만들 때 함께 하는 것이 자연스럽다.
- 관심 종목 낙관적 갱신을 허용하는 것이 "금액 화면 낙관적 갱신 금지" 원칙과 충돌하지 않는지. **별 아이콘은 금액이 아니므로** 허용이 맞다.
- `MarketPreview` 접힘을 CSS로만 처리할 수 있는지. 접힌 상태에서 차트를 마운트하지 않으려면 JS 판단이 필요할 수 있다 → **마운트 후 판단**(FR-31).

## Changelog

| 날짜 | 변경 |
|---|---|
| 2026-09-23 | **FR-62 · FR-63 구현.** 로그인이 MSW 목(`POST /api/v1/auth/login`)을 부르고 있던 것을 실제 계약(`POST /api/auth/login`, BFF 프록시 · `{email,password}` → `{user,accessToken,refreshToken}`)으로 바꿨다. 401 이면 `apiFetch` 가 갱신하고 1회 재시도하며, 리프레시까지 거부되면 세션을 비우고 로그인으로 보낸다. FR-63 문구는 "아직 계정이 없나요? / 초대 코드로 시작하기" 로 **바꿨다**(계정이 없는 사람이 읽는 문장). 근거 `reports/checklists/FE-REQ-011.md` §10 |
| 2026-09-21 | **실시간 수신 표시를 구현하고 `in-progress` 로 옮겼다.** 닫힌 것: FR-11 · FR-12("연결 끊김 · 재연결 중", 기준 시각을 쓰지 않는다) · FR-13(헤더를 `RealtimeAsOf` 로 떼어 표가 리렌더되지 않는다) · FR-14(`FE-REQ-010` FR-2 에서). **FR-10 은 다르게** — 수신 시각을 `entities/market/model` 이 아니라 `wsClient.getLastMessageAt()` 에 둔다. 수신 시각은 시세가 아니라 연결의 사실이고 `shared/api` 가 그 연결을 소유한다. **남은 것**: FR-1~5 · FR-20~73 중 미구현분. 근거: `requirements/reports/checklists/F000-realtime-reliability.md` |
| 2026-09-21 | **터치 선택 절 판정.** FR-23 pass(`(hover: hover)`) · FR-30 유지. **FR-22 는 다르게** — 선택 행에 색을 칠하지 않는다(05b0427 사용자 결정, 팔레트 밖 보라). 선택은 우측 프리뷰가 말하고 `aria-selected` 를 둔다 — 그 값이 `memoKey` 누락으로 첫 행에 멈춰 있던 것을 고쳤다. 같은 작업에서 `MarketOrder.Ascending` 이 빈 문자열이던 것(오름차순 = 내림차순)과 기간 변동률 표시를 고쳤다. 근거: `requirements/reports/checklists/F000-market-table.md` |
| 2026-09-21 | **스토리보드 갭 감사 반영.** 추가: FR-6 · 7(★ 단일 소스에 검색 포함 · 보유 ★ 해제 금지 — D8) · FR-80~84(검색 동작 — 디바운스 · 취소 · URL 동기화 · 최근 검색 로컬) · FR-85~88(북마크 — 낙관적 · 단일 소스 · 알림과 분리, D5) · FR-90~92(목표 수량 폼, 기본안 B5). 개정: FR-5 상한이 실제로 생겼다(추적 10, `409 TRACKED_ASSET_LIMIT`). 근거: `pm/requirements/reports/feature-audits/2026-09-21-storyboard-gap.md` |
