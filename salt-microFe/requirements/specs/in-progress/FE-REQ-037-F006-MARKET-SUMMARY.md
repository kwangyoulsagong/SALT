---
id: FE-REQ-037
feature: F006
area: fe
kind: UI
title: "투자 화면 시장 요약 띠 — 서버가 정한 대표 1 + 항목"
priority: high
labels: [fe, market, realtime, sparkline, design]
created: 2026-09-22
---

## Summary

`/investments`(자산 탭 `시장`) 제목과 탭 사이에 **시장 요약 띠**를 둔다. 왼쪽은 대표 종목 하나(이름 · 가격 · 등락 금액 ·
칸을 채우는 영역 차트), 오른쪽은 작은 항목들(작은 영역 차트 + 이름 · 태그 · 가격 · 등락)이 3줄씩 열을 이룬다.
**무엇을 보여 줄지 · 태그 · 등락 금액은 서버가 정한다**(`SRV-REQ-036` · `BFF-REQ-035`). 프론트는 받은 것을 그린다.
`FEATURE-006` FR-64~67 의 프론트 구현이다.

## 왜 — 관찰된 문제

| # | 문제 | 근거 |
|---|---|---|
| 1 | 화면에 들어와 **시장 전체가 어떤지**를 한눈에 볼 자리가 없다. 표 100행을 훑어야 BTC · ETH 방향을 안다 | 2026-09-22 사용자 요청 — 참고 화면의 상단 띠를 코인 버전으로 |
| 2 | 표는 거래대금 순이라 대표 종목의 **행 위치가 계속 바뀐다** | 기본 목록 실측: XRP · BTC · ETH · USDT · ZETA … |
| 3 | `@repo/ui/sparkline` 사용처 0, 선만 그린다 | 참고 화면은 영역 채움 + 점선 기준선 |

## 설계 원칙

- **서버가 정한다.** 처음 구현은 프론트에 종목 목록(BTC · ETH · XRP · SOL)과 5% 임계를 박았다 — 사용자 지적
  ("summary 는 서버에서 줘야지, 그냥 정해버리면 안 돼")으로 걷었다. 종목 · 순서 · 태그 · 등락 금액 · 스파크라인이 응답 하나로 온다
- **금액을 계산하지 않는다.** 등락 금액은 서버 값, 실시간 갱신은 거래소 값(BFF 가 WS 에 싣는다). 옛 BFF 라 없으면 이전 값을 둔다
- **태그는 방향을 말하지 않는다.** 코드 `wide_move` → 문구 `변동 큼`. "급등 · 급락" 0건(`FEATURE-006` FR-66)
- **치수는 참고 화면 실측**(2026-09-22): 띠 176 · 작은 항목 56 · 작은 차트 56×40 · 글자 11~14px · 선 1.6 · 회색 점선 기준선.
  첫 구현(카드 테두리 · 큰 글자 · 성긴 칸)은 사용자 판정 "디자인 별로"로 버렸다
- **변경 금지 목록(`FEATURE-000` E절)을 건드리지 않는다** — 표 · 필터 · 2컬럼은 그대로, 띠는 그 **위**다

## Requirements

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1 | 자리: `MarketBoard` 제목과 탭 사이. **두 탭 모두**에 보인다 | Must |
| FR-2 | 조회 `GET /api/app/market/summary` 한 번. 종목 · 순서는 응답 그대로(대표 `featured` + `items`). 프론트에 종목 목록 · 임계 상수 0 | Must |
| FR-3 | 대표: 로고 18 · 이름 13 · 가격 14 + `±금액 (변동률%)` · 영역 차트(폭 칸 전체 × 77). 작은 항목: 56×40 영역 차트 + 로고 16 · 이름 12 · 태그 · 가격 14 + `±금액 (변동률%)`. 스파크라인 `aria-label` 에 이름 · 구간(서버 `sparklineWindowMinutes`) · 오름/내림 · "점선은 구간 시작 값" | Must |
| FR-4 | 태그: 서버 코드 → 문구(`wide_move` → `변동 큼`, 회색 알약 11px). 태그 붙은 작은 항목은 변동률 방향 색으로 옅게 칠한다 | Must |
| FR-5 | 실시간: 가격 · 변동률 · 등락 금액이 WS 로 갱신된다 — 관심 종목 탭에서도. 응답은 1분마다 다시 받는다(스파크라인) | Must |
| FR-6 | 항목 = 링크 → 상세 분석(`ROUTES.investmentDetail`). 포커스 표시 유지 | Must |
| FR-7 | 배치: PC 대표 칸 240px · 항목 열 280px(3줄씩, 서버 기본 9개 = 3열) · 오른쪽 상자(남는 폭, 최소 220). 1379px 이하는 상자를 숨긴다. 767px 이하는 대표 + 항목 가로 스크롤 한 줄 | Must |
| FR-8 | 상태: 로딩 = **실제 칸과 같은 배치 · 폭**의 스켈레톤(대표 · 항목 · 상자, CLS 0). 조회 실패 = 띠를 그리지 않는다. 스파크라인만 없음 = 그 자리 비움 | Must |
| FR-10 | 대표 칸 아래 줄: 고가 · 저가 · 거래대금(거래대금은 `조 · 억 · 만` 표기). 오른쪽 상자 "오늘의 시장": 상승 · 보합 · 하락 종목 수(서버가 센다) + 얇은 막대(`flex-grow` = 수) + 대표 종목 최근 뉴스 3줄(서버가 중복 제목을 합쳤다). 해석 문구 0 | Must |
| FR-9 | 번들: `/investments` First Load 가 늘지 않는다 — 띠는 `ssr:false` 잎 | Must |

## 범위 밖

| 항목 | 사유 | 언제 닫히나 |
|---|---|---|
| 한 줄 사유 태그("ETF 순유입" 등) | 서버 생성 · 문구 검수 대상(공통 수용 기준 4) | F004 서버 후속 |
| 지수 · 환율 항목 | 데이터 소스 없음 · 자산군 결정 대기(`FEATURE-006` Q2 · Q7) | Q2 · Q7 결정 후 |
| 주요 일정 · 김치 프리미엄 · 시장 공포 · 탐욕 | 서버 소스 없음 / F003 소관 / 지금 값은 종목별이다 | 서버 명시 · F003 |
| 스파크라인 실시간 봉 병합 | 1분 재조회로 대신한다 | 필요 시 |

## Changelog

- 2026-09-22 신설 (사용자 요청 — 참고 화면 상단 띠의 코인 버전)
- 2026-09-22 개정 — **서버 소유로 전환**(사용자 지적). 종목 · 태그 · 등락 금액을 서버 응답으로, 프론트 상수 제거.
  디자인을 참고 화면 실측 치수로 다시(대표 1 + 작은 항목 3줄 열). 등락 금액이 범위 밖에서 범위 안으로
- 2026-09-22 개정 — 사용자 피드백 "상단 좌측이 여전히 크다 · 아이콘도": 대표 칸 240 · 차트 77 고정, 항목 열 260 고정, 코인 로고 추가
- 2026-09-22 개정 — 사용자 피드백 "너무 텅 비었다 · 우측에 설명 같은 걸": 항목 3열(서버 기본 9개), 대표 칸 아래 고가 · 저가 · 거래대금, 오른쪽 "오늘의 시장" 상자(FR-10). 상자 첫 안(두 줄 문장 + 굵은 막대)은 "구리다"로 수 세 칸 + 얇은 막대로. 스켈레톤을 실제 배치 · 폭과 같게. 항목 열 260 → 280(ETH 변동률 잘림)
