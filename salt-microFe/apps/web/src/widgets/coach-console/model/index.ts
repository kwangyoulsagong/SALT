/** 위젯이 소유하는 블록 이름 — `degradedFields` 의 필드 이름과 같다(`BFF-REQ-023` FR-11) */
export const REPORT_BLOCK_FIELDS = {
  recommendation: "recommendation",
  risks: "risks",
  exitPlans: "exitPlans",
  behaviorFacts: "behaviorFacts",
} as const;
