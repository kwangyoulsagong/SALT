/**
 * 네트워크 진입점.
 *
 * **HTTP 는 아직 여기 없다.** 현재 조회는 슬라이스(`entities/market/api`)가 `axios` 를
 * 직접 부르고, 목표·투자 요약은 MSW 가 받는다. BFF 가 생기는 `FE-REQ-012` 에서
 * 공통 클라이언트(토큰 부착·에러 정규화·타임아웃)를 여기에 만들고 슬라이스를 그 위로 올린다.
 */
export * from "./websocket";
