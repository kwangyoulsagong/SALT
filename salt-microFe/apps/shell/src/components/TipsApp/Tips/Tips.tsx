import { Wrapper } from "./Tips.css";
import { Header } from "@repo/ui/header";

const Tips = () => {
  return (
    <section className={Wrapper}>
      <Header></Header>
    </section>
  );
};
export default Tips;
