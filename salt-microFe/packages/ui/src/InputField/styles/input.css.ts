import { style, styleVariants } from "@vanilla-extract/css";

// 버튼 variants
export const inputVariants = styleVariants({
  primary: {
    width: "50px",
    height: "30px",
    background: "#7949FF",
  },
});
