# BFF-REQ-010 (F000 PERF) — 검증 체크리스트

- REQ: `requirements/specs/in-progress/BFF-REQ-010-F000-PERF.md`
- 브랜치: `feat/f000-invite-onboarding-slice`(#42) · `feat/f000-realtime-reliability`(#43) · `feat/f000-market-table`(#44)
- 검증일: 2026-09-18 ~ 2026-09-21 (근거 기록) · 이 문서는 2026-09-22 머지 후 백필
- 상태: **부분 완료** — A절(FR-1~4) · WS절(FR-10~15) 을 판정했다. 정리 효과 측정 · 홈 병렬화 · 관측성은 남았다
- **전 영역 통합 기록**: 루트 `requirements/reports/checklists/F000-realtime-reliability.md` ·
  `F000-market-table.md` · `F000-invite-onboarding.md` (실측 표 원본은 그쪽에 있다)

## 1. 변경 금지 목록의 성능 (FR-1~5)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-1 | `market/overview` p95 정리 전/후 동일 이상 | **pass** | BFF p95 **23.1ms** · 서버 17.8ms (`d959e39`). 덧씌우기가 없어져 늘 수 없다 |
| FR-2 | 응답 바이트 단위 동일 | **다르게** | 서버가 `periodChange` 를 더해(`4b72c9e`) 정리 전 응답과는 다르다. BFF 는 **서버 응답과** 바이트 동일(컨트롤러 테스트) |
| FR-3 | `limit=100` 유지 | **pass** | 루트 `F000-market-table.md` §3 |
| FR-4 | 가격 캐시 갱신 주기 · 히트율 측정 | **pass → 캐시 제거** | 히트율 **0%**(덧씌워진 행 0/100). 그 캐시는 워커 프로세스에서만 찬다 — `market-overview.service` 에서 덧씌우기를 걷어냈다(`d959e39`) |
| FR-5 | `market-intelligence` 프록시 성능 유지 | 미착수 | 측정 기록 없음 |

## 2. WebSocket (FR-10~15)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-10 | 구독 정책(`limit=100`) 유지 | **pass** | 구독 대상은 그대로다. `0483f14` 가 바꾼 것은 **전송 방식**(Upbit 에 항상 전체 집합)이다 |
| FR-11 | 가격 갱신 throttle | **다르게 — 만들지 않음** | 100심볼 14.8~19.5건/초 · 심볼당 ≤2.6건/초. 프론트가 rAF 로 묶는다. **수치가 10배가 되면 다시 본다** |
| FR-12 | 유령 구독 0건 | **부분** | 닫힌 소켓의 심볼은 구독 집합에서 빠진다(테스트). Upbit 쪽은 마켓 전체(289)를 늘 구독해 줄일 대상이 없다 |
| FR-13 | worker 직접 실행 시에만 시작 | 미착수 | `require.main === module` 가드는 `1a73839`(2026-05-24)부터 있다. F000 에서 검증하지 않았다 |
| FR-14 | 서버 `market-price-updater` 제거 후 BFF 워커 단일 경로 | 미착수 | 서버 `src/workers/market.worker.ts` 가 남아 있다 |
| FR-15 | 동시 연결 · 구독 심볼 · 전송률 측정 | **pass** | 루트 `F000-realtime-reliability.md` §2 BFF 표 |

같은 작업에서 시세가 **조용히 멈추는 경로 넷**을 고쳤다 — 연결 키 충돌 · Upbit 구독 대체 ·
서버 장애 시 0건(`0483f14`), SIGTERM 뒤 생존(`16f7eba`). 수정 전/후 수치는 루트 §2.

## 3. 정리 효과 측정 (FR-20~23)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-20 | 라우트 등록 수 전/후 | 미착수 | 동면 route 정리(`BFF-REQ-007` A절)가 선행 |
| FR-21 | 410 호출 수 1주 수집 | 미착수 | 410 응답 자체가 아직 없다 |
| FR-22 | `npm run build` 시간 전/후 | 미착수 | |
| FR-23 | 측정값을 이 문서에 남긴다 | **부분** | 이 문서가 생겼다. 담긴 것은 A절 · WS절 수치뿐이다 |

## 4. 현재가 보정 · 캐시 (FR-30~41)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-30 | 보정은 인메모리 읽기, 20ms 이내 | **미충족** | 관심 목록 보정(`app-watchlist.service` → `upbitWSService.getPriceCache()`)은 **REST 프로세스에서 늘 빈 캐시를 읽는다.** 서버 값으로 떨어져 겉보기 정상이다. 20ms 측정 없음 |
| FR-31 | 캐시 미스 시 서버 재호출 없음 | **pass** | 미스면 서버 값 + `priceStale` (`BFF-REQ-007` FR-42 테스트) |
| FR-32 | O(항목 수), 중첩 루프 0 | **pass** | `watchlist.viewmodel.ts` — `serverItems.map` 한 번 + `Map.get` |
| FR-40 · 41 | 새 캐시 0 · 두 곳 캐시 금지 | **pass** | F000 diff 가 더한 `Map` 은 rate limit 창과 요청 단위 이름 표뿐. 기존 캐시 하나를 **걷어냈다**(`d959e39`) |

## 5. 병렬화 · 타임아웃 (FR-50~53)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-50 | 홈 조립 `allSettled` | 미착수 | `BFF-REQ-007` B절 |
| FR-51 | 서버 호출마다 타임아웃 | 미착수 | `backend-api.service` axios `timeout` 10s/30s 는 `4b529dd`(2025-11) 부터 있다. F000 에서 검증하지 않았다 |
| FR-52 | mutation 재시도 0회 | 미착수 | 동일 — 재시도 코드가 없다는 것만 확인했다 |
| FR-53 | `invite/check` rate limit | **pass** | 창 60초 · check 30회. 34회 중 6회 429 (`a4f9cee`, `BFF-REQ-007` FR-63) |

## 6. 관측성 (FR-60~63)

| FR | 내용 | 판정 | 근거 · 위치 |
|---|---|---|---|
| FR-60 | 엔드포인트별 p95 · 410 수 · 캐시 히트율 · WS · 전송률 · `priceStale` 발생률 | **부분** | overview p95 · 캐시 히트율 · WS 연결/전송률만. 나머지 엔드포인트 p95 · 410 · `priceStale` 발생률 없음 |
| FR-61 | BFF 가 얹는 지연 별도 측정 | **부분** | overview 한 경로 p50 **1.5ms** (`d959e39`) |
| FR-62 | 초대 요청 본문(비밀번호) 로깅 없음 | **pass (코드)** | `error.middleware` 는 message·stack·url·method 만 찍는다. 본문 로거 없음. 로그 수집 실측은 없다 |
| FR-63 | 성능 수정 커밋에 before/after | **pass** | `d959e39`(히트율 0/100 · p95) · `0483f14`(0 → 41건/8초 · 0 → 19.5건/초) · `16f7eba`(40초 생존 → 0.2초) |

## 7. 2026-09-21 추가 수용 기준

| 항목 | 판정 | 비고 |
|---|---|---|
| 검색 · 북마크 경로 예산 · 검색어 로그 0건 | 미착수 | 경로가 아직 없다(`BFF-REQ-007` H·I절) |

## 8. 미검증 · 범위 밖

| 항목 | 사유 | 언제 닫히나 |
|---|---|---|
| 관심 목록 보정의 빈 캐시 | 겉보기 정상이라 고치지 않았다(루트 `F000-market-table.md` §5) | `watchlist` 뷰모델을 다시 만질 때 |
| FR-11 throttle 재판정 | 현재 수치에서 불필요 | 전송률이 10배가 될 때 |
| 410 1주 수집 · 라우트 수 | 동면 route 정리 선행 | `BFF-REQ-007` A절 |
| 운영 수치 | 배포 환경이 없다. 전부 로컬 측정이다 | 배포 환경 확정 시 |

## 9. 명령

`npm run build` pass · `npm test` 28(#42) → 32(#43, 연결 관리 +4) → **35**(#44, 컨트롤러 +3) pass.
lint 스크립트는 BFF 에 없다.
