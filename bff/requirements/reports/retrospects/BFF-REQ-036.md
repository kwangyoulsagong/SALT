# BFF-REQ-036 — 회고

- **메모의 목록(컨트롤러 2 + proxy)보다 실제가 컸다.** `error.response` 를 잡는 곳만 찾았는데 `status(500)` 으로
  뭉개는 곳이 5개 더 있었다 — "4xx 보존"을 찾을 땐 잡는 코드와 **삼키는 코드** 둘 다 grep 한다
- **스펙을 읽기 전에 구현했다.** 규칙(§7)만 보고 feed 를 지웠고 코드 이름을 지어냈다 — 본문은 `BFF-REQ-007` A절에
  있었다. 규칙은 요약이고 REQ 가 계약이다
- `app.ts` 를 테스트에서 import 하면 거래소 소켓이 열려 러너가 멈춘다. 테스트는 같은 마운트의 작은 앱으로 우회했다
- 규칙 두 개가 서로 반대로 읽힌 원인은 "구독"이 두 층(Upbit · 클라이언트)인데 한 단어로 적은 것이었다

## Action

- `upbit-ws.service` 연결을 명시적 `start()` 로 — BFF 실시간 REQ 에서(REST 프로세스 소켓 · 테스트 멈춤 둘 다 닫힌다)
- 2026-09-29 동면 로그 확인 → `DORMANT_PATHS` 삭제 여부 결정
