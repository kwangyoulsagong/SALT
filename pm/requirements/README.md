# PM Requirements

PM 기능 기획서와 제품 리포트를 관리한다.

## Directory

```text
pm/requirements/
  specs/
    to-do/
    in-progress/
    done/
  reports/
    checklists/
    retrospects/
    feature-audits/
```

기능 기획서는 `specs/`에 두고, 기능 감사/조사 리포트는 `reports/`에 둔다.
