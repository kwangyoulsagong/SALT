# 스토리보드 ↔ REQ 갭 감사와 결정 (2026-09-21)

원본: `pm/storyboard/SALT-Storyboard.html`(2026-09-08) · `pm/prototype/src/App.jsx`.
대조: PM `FEATURE-*` · 영역 REQ(`DB`·`SRV`·`BFF`·`FE`·`RN`) · 코드.
감사 4갈래: 투자 · 홈/코치/적립 · 청구서/세금/포트폴리오 · 공통(판정 46행 · 커버리지 · 상태 12종 · 토큰).

> **발단.** 투자 화면 우측 패널이 스토리보드에서는 **AI 코치 패널**인데 PM 기획서에는 FR-33
> "카드 1개"로만 내려왔고, 영역 REQ 에는 0건이었다. 사용자: *"스토리보드에는 있지 ·
> 모든 기능 req 다 작성해야돼 하나도 빠짐 없이"*.

## 1. 사용자 결정 (2026-09-21)

| # | 결정 | 대체하는 것 |
|---|---|---|
| D1 | **원장 · 청구서 · 세금(F001 · F002)을 뺀다.** REQ 42건 삭제 | `ADR-002` · 글로벌 플랜 §0·§3·§4·§9·§10 |
| D2 | **스마트 바이존을 살린다.** 보유 종목 = 서버가 계산한 **내 규칙 가격**(profit-plan: 손절 · 1차 · 2차). 미보유 종목 = **관찰 구간** — 가격 목표가 아니라 과거 가격 분포 기반 구간과 규칙 설명. 둘 다 서버 계산 · "예측 아님" 라벨 · 수익률 % 표기 없음 | `DB-REQ-017` FR-30(`smart_buy_zone` 생성 중단) |
| D3 | **단타 / 장기 두 모드를 노출한다. 신뢰도 % 는 뺀다** — 점수 + 근거 · 적중률 · 실패사례 3종으로 대체. 유효시간 표기는 서버 기준 하나로 | `FEATURE-004` FR-12(`defaultMode` 숨김) |
| D4 | 관심 종목 표에 **신호 컬럼을 두지 않는다.** 판단은 우측 패널 · 상세에서만 | 스토리보드 `watchlist` n2 |
| D5 | **알림은 1종(지표 · 추천 갱신).** 세금 D-Day 가 D1 으로 빠졌다. **뉴스 북마크는 알림이 아니라 별도 기능으로 살린다.** 알림 목록에 읽음 · 모두 읽음 · 안 읽은 수 | `FEATURE-006` FR-50(2종) · 스토리보드 "4종 유지 · 환율 함정 · 사용자 조건 알림" |
| D6 | **홈 = 3블록(총자산 → 이번 주 적립 → AI 추천) + 그 아래 기존 목표 카드 · AnalysisGraph · TipsApp** | `FEATURE-006` FR-10(5블록) · `FEATURE-000` FR-30(상단 2줄) |
| D7 | **`/investments` 는 자산 탭의 `시장` 세그먼트다.** 자산 탭 = `포지션` / `시장` / `관심 종목`. 종목 → **상세 분석 페이지**(push). PC 는 지금 2컬럼 그대로 | `FEATURE-006` FR-3(자산 세그먼트 = 포지션/청구서/세금) · `FEATURE-000` FR-31(투자 6탭) |
| D8 | **추적 자산 상한 10.** 보유 종목은 상한과 무관하게 늘 추적. 종목 검색으로 추가 | `FEATURE-000` UX 상태 "최대 5개" |
| D9 | **설정 = 한 화면. 적립 기본액만 편집, 밴드 임계값은 읽기 전용** — 과거 적중률이 기본 밴드 기준이라서 | 스토리보드 `settings` n2(임계값 편집) · n3(세금 파라미터 — D1 로 소멸) |
| D10 | **제품 화면의 브랜드 색은 `#007AFF`** — 앱 로컬 토큰(`apps/web/src/shared/ui/tokens.css.ts`)으로 쓴다. **디자인 시스템 토큰(`packages/tokens` · `@repo/ui`, `#7949FF`)은 바꾸지 않는다**(사용자: 바꾸면 앱 전체가 다시 칠해진다). 새 화면(AI 코치 패널 · 포지션)도 앱 로컬 토큰을 쓴다 | `FE-REQ-005` D-1 은 디자인 시스템 기본값으로 남는다 |
| D11 | **(Q5) 종목 판단의 실패 이력은 `symbol_judgment` 스냅샷(B30)으로 쌓는다.** 종목 경로 `<mode>.<action>` 8종의 성적표 · 실패사례는 스냅샷의 사후 결과에서 오고 **`IndicatorTrackRecord`(밸류에이션 지표)를 쓰지 않는다** — 판단 근거(RSI · 심리 · 대량 체결)와 다른 신호의 실패를 붙이면 오해를 부른다. 표본 < 20 이면 ② 는 `insufficient_sample` 로 미렌더(표본 N건 표시), ③~⑥ 은 그대로 | `SRV-REQ-024` D절 "실패 이력" 열 · `DB-REQ-019` FR-45 의 종목 8행 |
| D12 | **(Q3) 미보유 관찰 구간은 ETF · 지수 · 코인만.** 미보유 개별 주식은 `unavailable(out_of_scope)` 사유 한 줄 — 글로벌 플랜 §1-3 과 일치. 보유 개별 주식의 내 규칙 가격(`held_rule`)은 그대로 | `scope_undecided` 보수안 → 확정 |
| D13 | **(Q6) 현재가와의 거리는 가격 차이만** — 종목 호가 통화의 금액(`priceGap`, 부호 있음). **거리 % 를 싣지도 그리지도 않는다** — % 는 수익률로 읽힌다(D2 · 공통 기준 ④) | `distancePct` · `distanceFromCurrentPct` → `priceGap` · `gapFromCurrent` |
| D14 | **`FE-REQ-010` FR-53: 투자 화면 표 높이를 뷰포트에 맞춘다.** 페이지는 세로로 스크롤되지 않고 표 · 프리뷰가 각자 스크롤한다 | 체크리스트 `F000-market-table` §5 대기 항목 |

## 2. 기본안 (결정 전 기본값 — 바꾸려면 이 표를 고치고 REQ 를 따라 고친다)

| # | 기본안 | 근거 |
|---|---|---|
| B1 | **주문 전 체크**: 외부 주문 링크 · 버튼 없음(`FE-REQ-026` FR-53 유지). 목표가 입력은 **빈칸**(시스템이 채우지 않는다). 금액 · 손절 칩, "총자산 대비 최대손실" 항목 추가, PC 는 상세 페이지 상주 카드 | 공통 기준 ②·④ |
| B2 | **실패 이력**: 카드에서는 요약 1건, 상세에서는 적중과 **같은 비중**(아코디언에 숨기지 않는다) | 공통 기준 ① |
| B3 | **Gemini 해설**: 예상 수익 범위는 폐기(서버에서 이미 제거). 뉴스 5줄 요약은 추가 | 공통 기준 ④ |
| B4 | **투자 피드**: 동면 유지 | `FEATURE-000` FR-12 |
| B5 | **수량 목표**("BTC 1개 모으기"): Should. 목표 카드 시각은 그대로, 입력만 추가 | 스토리보드 `goals` |
| B6 | **TipsApp 개인화**: Could. 문구 검수 대상 | 스토리보드 `asIsHome` n3 |
| B7 | **PC 홈 최대 폭**: Should | 스토리보드 `asIsHome` n6 |
| B8 | ~~브랜드 색 확정~~ → **Q4 로 옮김.** 디자인 시스템 토큰은 `#7949FF`(`FE-REQ-005` D-1, done), 투자 화면 앱 로컬 팔레트는 `#007AFF` 라 한쪽을 옛 값으로 볼 근거가 없다 | — |
| B9 | **게이지 아래 적중률 한 줄**: 서버 신규 집계(`MarketSentiment` 이력 + `PriceHistory` → 구간 진입 후 30일 수익률 분포). 표본 < 20 이면 배지 | 스토리보드 `asIsInvestment` n8 |
| B10 | **심볼 경로에도 3종 게이트**: `GET /api/ai-coach?symbol` 응답에 `renderable` · `blockedReason` · 적중률 · 실패사례 | 공통 기준 ① |
| B11 | **뉴스**: 기사 감정 배지 + 종목 연결(→ 상세). BFF 뷰모델이 `sentiment` · `symbols` 를 버리지 않는다 | 스토리보드 `news` n1 |
| B12 | **온보딩 2단계** "계좌 연결" → **"첫 보유 기록 입력"**(건너뛰기 가능). 3단계 적립액은 실제로 저장(F003) | D1 · 계좌 연동은 영구 Non-Goal |
| B13 | **포지션 세그먼트**: Hero(총액 · 수익 · 원금 · 보유 수 · 최대 비중 · 30일 수익률) · 평가금 흐름(1주/1개월/3개월/1년 + 기간 최대 낙폭) · 보유 목록 · 리스크 레이더 **4축**(집중도 · 변동성 · 낙폭 · 뉴스 리스크 — 현금 비중은 입금 기록이 없어 뺀다) · [거래 기록 추가] 바텀시트 · 거래 목록/수정/삭제 | 스토리보드 `portfolio` · `sheetHolding` |
| B14 | **거래 기록 추가**: 자산군 → 종목 → 매수/매도 → 수량 → 단가 → 거래일. 평단 전후는 **서버 미리보기**. 기존 `PortfolioTransaction` API(원장 확장 없음) | D1 · 공통 기준 ③ |
| B15 | **코치 탭**: 대화 + 추천 카드. F004 섹션(리스크 · 후보 · 익절 · 성적표 · preflight · 행동 기록)은 **코치 리포트** push 화면 | 스토리보드 `coach` · `coachDetail` |
| B16 | **`defaultMode` · `notificationLevel`**: 노출(D3 과 짝). 컬럼 추가 | `FEATURE-004` FR-12 ↔ `FE-REQ-026` FR-81 충돌 해소 |
| B17 | **코치 상세**: 신호 유형별 신호 후 30일 수익률 **분포** | 스토리보드 `coachDetail` n1 |
| B18 | **`signalType` ↔ `signal-performance` 매핑 표**를 서버 REQ 에. 표본이 없어 전부 미렌더인 초기 상태를 정상 UX 로 | 스토리보드 `coach` 선결 과제 |
| B19 | **사용자 조건 알림 · 알림 만들기 시트 · 환율 함정 알림 · 보류 기능 토글**: 범위 밖 | D5 · FOMO 방지 |
| B20 | **F003**: 원장 자동 매칭 제거(수동 체크만). 목표 화면 상단 **이번 달 적립 합계**(`WeeklyPlanExecution`) | D1 · 스토리보드 `goals` n1 |
| B21 | **F007**: 세금 D-Day 푸시 제거. 푸시 = 지표 · 추천 갱신 1종 | D1 · D5 |
| B22 | **투자 화면 UX 상태**(loading · error · partial) 절을 `FE-REQ-010` 에. 웹 오프라인은 WS "연결 끊김" 표시로 갈음한다고 명시 | 스토리보드 `states` |
| B23 | **상세 분석 페이지 Hero**: 관심 추가 · 주문 전 체크. "알림 만들기"는 B19 로 범위 밖 | 스토리보드 `detail` n1 |
| B24 | **북마크한 뉴스 목록은 `관심 종목` 세그먼트 아래 블록.** 스토리보드는 알림 탭이었지만 D5(북마크는 알림이 아니다) | 스토리보드 `news` n2 · D5 |
| B25 | **추적 자산 = 관심 종목 ∪ 보유 종목.** 새 테이블 · `MarketAsset.isTracked` 없음(추적은 사용자별). 11번째 추가는 `409 TRACKED_ASSET_LIMIT`. 보유를 팔아 10을 넘게 되면 자동 제거하지 않고 새 등록만 막는다. 검색은 별도 경로(`GET /api/investment/search` · BFF `/api/app/search`) — overview 스냅샷을 깨지 않게 | D8 |
| B26 | **상세 분석 페이지가 생기기 전 임시 목적지**: 뉴스 칩 · 검색 행 → `/investments?symbol=`. 현재 웹 코드가 이 쿼리를 읽지 않아 작은 수리가 필요 | D7 · `FE-REQ-010` Open Questions |
| B27 | **수량 목표 진행률은 저장하지 않고** 보유 수량으로 계산한다 | B5 |
| B28 | 뉴스 `isBookmarked` 는 **인증 요청일 때만** 서버가 붙인다 | B11 · D5 |
| B29 | **`/api/app/ai-coach/detail` 은 종목 판단(패널 · 상세)용.** 코드가 이미 그렇다(`app-ai-coach.service.ts` `getDetail`). 코치 리포트는 새 `/api/app/coach/report` | B15 |
| B30 | **종목 판단 스냅샷**(`symbol_judgment` + 워커)을 저장한다 — 요청 때 계산하고 버리면 종목 경로 적중률 표본이 쌓이지 않는다 | B10 |
| B31 | 관찰 구간 파라미터: 과거 가격 **20 · 50 · 80 백분위**, 단타 = 24시간 5분봉, 장기 = 1년 일봉 | D2 |
| B32 | 현금 기록이 없으므로 주문 전 체크의 "전액" 칩과 "잔여 현금" 항목을 뺀다 | B13 |
| B33 | `notificationLevel` = 알림 1종(D5)의 빈도. 마지막으로 쓴 모드는 **기억하지 않는다**(URL 이 유일한 저장소) | D3 · D5 · B16 |
| B34 | **알림 설정은 두 필드.** `User.alertsEnabled` = 전체 켜기/끄기(F006 · F007 푸시도 따른다), `notificationLevel` = 켜져 있을 때의 빈도(F004). 끄면 빈도와 무관하게 0건 | B16 · D5 · 2026-09-21 영역 간 대조 |
| B35 | 코치 대화 추천 질문 Chip "세금 언제까지?" → **"내 포트폴리오 위험한가?"** | D1 |
| B36 | 온보딩 단계 키 **`link_account` → `first_holding`** (BREAKING — 서버 · BFF · FE · RN 동시). 알림 읽음만 낙관적 갱신 허용(금액 아님) | B12 |
| B37 | **월 → 주 환산은 ×12 ÷ 52, 1,000원 반올림**(월 130만 → 주 30만). 스토리보드는 ÷4(주 32.5만)였다. 기본액 범위 1만 ~ 1억원 · 기본 배분 비율 · 월 합계의 달 귀속(`weekOf` 기준)은 제안값 | D9 |
| B38 | **"월요일 아침 적립" 푸시는 없다.** 알림 1종(D5)이라 밴드가 바뀐 주에만 `signal_update` 가 간다 | D5 · B21 |
| B39 | **종목 판단 스냅샷의 적중 판정**(D11). 관찰 기간은 모드의 유효시간을 따른다 — 단타 24시간 · 장기 30일. `review_*` = 기간 수익률 > 0 이면 적중, `avoid` = ≤ 0 이면 적중, `wait` = 절댓값이 단타 2% · 장기 10% 안이면 적중. 같은 종목 · 모드 표본은 관찰 기간 안에서 **1건만** 센다(시간 버킷이 겹쳐 표본이 부풀지 않게) | D11 · `SRV-REQ-026` Open Question(표본 독립성) |

## 3. 열린 질문 (결정 전 — 코드를 건드리지 않는다)

| # | 질문 | 막는 것 |
|---|---|---|
| Q1 | `apps/web-tax` zone 을 걷을지(단일 zone) 다른 경계로 쓸지 | ADR-001 구조 · `FE-REQ-007` 후속 |
| Q2 | 자산군 3종(`AssetType` · KIS)을 유지할지 — 주된 근거가 세금 차이였다 | `DB-REQ-003` · `SRV-REQ-008` FR-32 · `FEATURE-000` FR-34/35 |
| ~~Q3~~ | ~~미보유 주식 종목의 관찰 구간~~ → **D12 로 결정(ETF · 지수 · 코인만)** | — |
| ~~Q4~~ | ~~브랜드 색~~ → **D10 으로 결정(`#007AFF`)** | — |
| ~~Q5~~ | → **D11 로 결정(스냅샷으로 쌓는다).** 원래 질문: **종목 경로 3종 게이트가 늘 막힐 수 있다.** 실패 이력은 `IndicatorTrackRecord`(밸류에이션 지표 · F003)에서 오는데 종목 판단 점수는 RSI · 심리 · 대량 체결로 매긴다. 판단 신호별 실패 이력을 새로 쌓을지(B30 스냅샷 기반), 쌓일 때까지 패널 ② 를 "표본 부족"으로 둘지 | 우측 패널 ② · 상세 코치 카드 |
| ~~Q6~~ | → **D13 으로 결정(가격 차이만).** 원래 질문: 바이존 · 관찰 구간의 **현재가 대비 거리 %** 를 남길지. D2 는 수익률 % 표기를 금지했고, 지금 REQ 는 "가격 간격이지 수익률 아님" 라벨로 남겼다 | `FEATURE-004` FR-6 |
| Q7 | **`fx`(환율 · 김프)의 주인.** 김프에 쓰는 환율은 삭제된 `SRV-REQ-014`(F001)가 만들던 것이다. F006 `SRV-028` · `030` 이 전부 덮는지, F003 이 가져갈지 | F003 김프 · 총자산 원화 합산 |
| Q8 | **F003 기존 불일치(이번에 생긴 것 아님)**: `FE-REQ-024` · `RN-REQ-018` 의 `/bff/plan/*` · `planId` · `totalAmount` 가 `BFF-REQ-020` 의 `/api/app/plan/*` · `totalKrw` 와 다르다 | F003 착수 시 첫 정리 |

## 4. 갭 목록 → 넣을 곳

| 스토리보드 | 항목 | 넣을 곳 |
|---|---|---|
| `asIsInvestment` n6 · `detail` | 우측 AI 코치 패널(①모드 ②판단 ③바이존/관찰 구간 ④⑤게이지 ⑥뉴스 ⑦상세 이동) · FR-33 을 F004 로 이관 | F004 전 영역 |
| `asIsInvestment` n8 | 게이지 아래 적중률 한 줄 (B9) | F004 SRV · DB · BFF · FE |
| `asIsInvestment` n9 · `detail` · `asset` | 상세 분석 페이지(Hero · 차트 + 오버레이 · 코치 카드 · 해설 · 수익 플랜 · 주문 전 체크) | F004 전 영역 · RN |
| `sheetPreflight` | B1 | F004 FE · BFF |
| `watchlist` | 신호 컬럼 없음(D4) — 범위 밖 명시 | F004 · F000 |
| `search` · `settings` | 종목 검색 + 추적 10 (D8) | F000 전 영역 |
| `news` | 감정 배지 · 종목 연결(B11) · 북마크(D5) | F000 SRV · BFF · FE · RN |
| `alerts` | 목록 · 읽음 · 모두 읽음 · 안 읽은 수(D5) | F006 전 영역 |
| `home` · `asIsHome` | D6 · B6 · B7 | F006 |
| `portfolio` · `sheetHolding` | B13 · B14 | F006 전 영역 |
| `settings` | D9 · B16 | F006(화면) · F003(적립액) · F004(성향 · 모드) |
| `onboarding` | B12 | F006 · F003 |
| `coach` · `coachDetail` | B15 · B17 · B18 · B2 | F004 |
| `goals` | B5 · B20 | F000(수량) · F003(합계) |
| `states` | B22 | F000 FE |
| `tokens` | D10 · SegmentedControl · RadarChart(B13 이 소비처) | `FE-REQ-005` · F006 FE |
| `scope` | 레거시 모델 · 중복 worker · 동면 410 · `AssetType` — 이미 `SRV-REQ-007` · `SRV-REQ-010` 에 있고 **코드 미완** | 기존 REQ 그대로 |

## 5. 대체된 것 (쓰지 않는다)

5탭 하단 내비 · `/investments` 6탭 · 청구서/세금 탭 · 홈 5블록 중 ④⑤ · 알림 4종 · Gemini 예상 수익 범위 ·
투자 피드 구현 · 사용자 조건 알림 · CSV/계좌 연결 · 세금 법령 파라미터 편집 · 증빙 아카이브.

## 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-09-21 | 최초 작성. 결정 9 · 기본안 22 · 열린 질문 4 (B8 → Q4) |
| 2026-09-21 | Q4 → **D10 브랜드 색 `#007AFF`** (사용자 결정). 같은 날 **디자인 시스템 토큰은 교체하지 않는다**로 좁혔다 |
| 2026-09-21 | Q3 · Q5 · Q6 · FR-53 → **D11 ~ D14** (사용자 결정). 적중 판정 세부는 기본안 B39 |
