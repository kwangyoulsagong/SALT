# FEATURE-007: 모바일 앱 (React Native · iOS + Android)

## TL;DR

- **마감일 알림이 없으면 이 제품의 약속을 못 지킨다.** 세금 마감(F002)은 D-30/14/7/3/1 알림이 기능의 일부이고, 웹만으로는 그것이 도달하지 않는다.
- `salt-microFe/apps/mobile`에 **React Native** 앱을 만든다. iOS와 Android를 하나의 코드로.
- IA는 웹과 같다 — **3탭**(`홈`/`코치`/`자산`). 세금은 자산 탭 내부 스택(웹의 별도 zone과 다르다).
- **비공개·초대제이므로 공개 스토어 출시를 하지 않는다.** iOS는 TestFlight, Android는 Play 내부 테스트.
- **웹 디자인 시스템을 재사용할 수 없다.** vanilla-extract는 RN에서 동작하지 않는다 → `packages/tokens` 추출 + `packages/ui-native` 신설.

## 배경과 문제

### 왜 모바일이 필요한가 — 결정에 시점이 있다

이 제품이 답하는 결정 네 개는 전부 시점이 있다.

| 결정 | 시점 | 모바일이 필요한 이유 |
|---|---|---|
| 이번 주 얼마 넣을까 | 월요일 아침 | 데스크톱 앞이 아니다 |
| 지금 뭘 할까 | 시장이 움직일 때 | 알림을 받고 즉시 본다 |
| **언제까지 팔아야 하나** | D-Day 임박 | **푸시가 없으면 마감을 놓친다** |
| 내가 뭘 잘못했나 | 매매 직후 | 거래소 앱에서 나온 직후다 |

특히 세 번째가 결정적이다. **2026-12-29~31은 1년에 딱 하루~이틀의 창**이고, 놓치면 250만원 공제 한 해분과 손익통산 기회가 사라진다. "웹에 들어와서 확인하세요"로는 그 창을 못 지킨다.

### 왜 PWA로는 안 되는가

**iOS의 웹 푸시 제약**이 핵심 기능을 막는다. 홈 화면 추가를 사용자에게 요구하는 것도 초대제 10명에게조차 마찰이다. 상세 비교는 `RN-REQ-001`.

### 왜 React Native인가

- 기존 React/TypeScript 역량을 그대로 쓴다.
- `entities`/`model`·BFF 뷰모델 타입·포맷터를 **`packages/core`로 공유**할 수 있다.
- Flutter는 Dart를 새로 배워야 하고 도메인 타입까지 두 번 정의하게 된다.

## 목표

1. **세금 D-Day와 지표/추천 갱신 알림이 도달**하게 한다.
2. iOS·Android에서 3탭 IA를 웹과 같은 정보 구조로 제공한다.
3. **스토어 심사 없이 배포**할 수 있게 한다 (OTA).
4. 웹과 **시각·문구가 갈라지지 않게** 한다 (토큰 공유).
5. 오프라인에서 마지막 상태를 볼 수 있게 한다.

## 사용자 시나리오

1. 초대 링크로 TestFlight(또는 Play 내부 테스트) 초대를 받아 앱을 설치한다.
2. 앱을 열면 **초대 코드**를 입력한다. 그다음 계좌 연결(업비트 CSV / KIS), 월 적립액. 3스텝으로 끝난다.
3. 홈에 총자산·이번 주 적립·AI 추천·세금 D-Day·청구서 한 줄이 보인다.
4. **12월 15일 아침, 푸시가 온다** — `미국주식 손실 수확 D-14 · 12/29까지`.
5. 알림을 누르면 세금 화면으로 **딥링크**한다. 손실 수확 후보 3개가 보인다.
6. `코치` 탭에서 `"이거 팔면 세금이 얼마 줄어?"` 라고 묻는다. 답이 흐르고 근거·적중률·실패사례가 붙은 카드가 온다.
7. 실제 매도는 증권사 앱에서 한다. SALT는 계산과 근거까지다.
8. 지하철에서 앱을 연다. 네트워크가 없다. **마지막 갱신 시각과 함께 캐시된 화면**이 보이고 금액에 "오프라인" 배너가 붙는다.
9. 며칠 뒤 계산 버그가 수정된 릴리스가 **스토어 심사 없이** 적용된다.

## 기능 요구사항

### A. 앱 기반

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-1 | `apps/mobile` 신설. **iOS와 Android 실기기에서 기동**한다 | Must | Draft |
| FR-2 | IA는 웹과 같은 **3탭**(`홈`/`코치`/`자산`). **4개로 늘리지 않는다** | Must | Draft |
| FR-3 | 세금은 **자산 탭 내부 스택**이다. RN에는 zone 개념이 없다 | Must | Draft |
| FR-4 | **`packages/tokens`(플랫폼 중립) + `packages/ui-native`.** 웹 `packages/ui`를 재사용하지 않는다 — vanilla-extract는 RN에서 동작하지 않는다 | Must | Draft |
| FR-5 | 컴포넌트 이름을 웹과 같게 맞춘다. **코드 공유가 아니라 사람이 두 앱을 넘나들 때 검색이 되게** 하기 위함이다 | Must | Draft |
| FR-6 | 내부 구조는 **FSD**이고 슬라이스 이름을 웹·서버와 공유한다. 모바일 전용 슬라이스는 `device` 하나 | Must | Draft |

### B. 푸시 알림 — 이 기능의 존재 이유

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-10 | **디바이스 등록**: 앱이 푸시 토큰을 서버에 등록한다. 사용자당 여러 기기 허용 | Must | Draft |
| FR-11 | **알림 타입은 2종**이다: `tax_deadline` · `signal_update`. 그 외를 만들지 않는다 | Must | Draft |
| FR-12 | **세금 D-Day 알림**: 자산군별 D-30/14/7/3/1에 각 **1회**. 발송 기록으로 중복 방지 | Must | Draft |
| FR-13 | 알림 문구에 **압박 표현을 쓰지 않는다.** "서둘러 파세요" ❌ / "연내 결제 기준 마감일: 12/29" ⭕ | Must | Draft |
| FR-14 | 알림을 누르면 해당 화면으로 **딥링크**한다. 딥링크 파라미터를 신뢰하지 않고 진입 후 서버로 재검증한다 | Must | Draft |
| FR-15 | 사용자가 알림 타입별로 끌 수 있다. **끈 상태에서도 D-1은 보낸다** — 되돌릴 수 없는 마감이므로. 그 예외를 설정 화면에 명시한다 | Should | Draft |
| FR-16 | **알림에 금액을 담지 않는다.** 잠금 화면에 자산 금액이 뜨면 안 된다. "미국주식 손실 수확 D-14" 까지만 | Must | Draft |
| FR-17 | 푸시 발송 실패(토큰 만료·기기 삭제)를 처리하고 죽은 토큰을 정리한다 | Must | Draft |

### C. 앱 버전 게이트

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-20 | 앱이 자기 버전(네이티브 + OTA 런타임)을 서버에 보고한다 | Must | Draft |
| FR-21 | 서버가 **최소 지원 네이티브 버전**을 응답한다. 앱이 그보다 낮으면 **업데이트 안내 화면**을 띄우고 진행을 막는다 | Must | Draft |
| FR-22 | **네이티브 변경을 OTA로 내보내지 않는다.** 릴리스 체크리스트가 이 판정을 강제한다 | Must | Draft |

### D. 오프라인

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-30 | 홈 5블록의 마지막 성공 응답을 캐시하고 **`마지막 갱신 시각`을 표시**한다 | Must | Draft |
| FR-31 | 금액 화면(청구서·세금·포지션)은 캐시를 보여주되 **"오프라인 · 시각 기준" 배너**를 붙인다 | Must | Draft |
| FR-32 | **오프라인 큐를 만들지 않는다.** 나중에 자동 전송되면 사용자가 결과를 확인하지 않은 채 상태가 바뀐다. 실패를 알리고 다시 누르게 한다 | Must | Draft |
| FR-33 | 코치 대화는 오프라인에서 스트림을 시작하지 않고 **입력을 보존**한다 | Must | Draft |

### E. 보안

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-40 | **토큰을 `AsyncStorage`에 두지 않는다.** iOS Keychain / Android Keystore | Must | Draft |
| FR-41 | **거래소 API 키를 앱에 저장하지 않는다.** 등록은 서버가 하고 앱은 마스킹 상태만 본다 | Must | Draft |
| FR-42 | 스크린샷 방지·루팅 탐지를 하지 않는다 — 비공개 초대제 앱이고 지키는 것보다 복잡도가 크다 | Must | Draft |
| FR-43 | CSV 업로드가 파일 접근 권한을 요구한다. **권한 요청을 최소화하고 사유를 명시**한다 | Must | Draft |

### F. 배포

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-50 | iOS는 **TestFlight**, Android는 **Play 내부 테스트**. **공개 스토어 출시를 하지 않는다**(글로벌 플랜 1-1절) | Must | Draft |
| FR-51 | 테스터 목록을 **초대 코드 발급 대상과 일치**시킨다 | Must | Draft |
| FR-52 | **OTA로 JS를 배포**한다. 네이티브 변경이 없으면 스토어를 거치지 않는다 | Must | Draft |
| FR-53 | 금액·세금 계산이 바뀌는 릴리스는 **점진 롤아웃** 필수. 롤백이 명령 하나 | Must | Draft |
| FR-54 | 스토어 등록 정보에 **투자 조언·수익 보장 표현을 쓰지 않고** 면책을 포함한다 | Must | Draft |

## 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 성능 | **콜드 스타트 2s(저가 Android 실기기)** · 웜 600ms · 탭 전환 200ms · 리스트 60fps · 코치 첫 토큰 1s · **JS 번들 4MB 이하** · OTA 다운로드 3s(LTE) |
| 접근성 | 터치 타깃 44×44 · `accessibilityRole`/`Label` · 손익은 색 + 부호 문자 · **폰트 확대에서 금액이 잘리지 않는다** · VoiceOver·TalkBack으로 총자산·추천 카드 낭독 |
| 보안 | FR-40~43. 알림에 금액 없음 |
| 장애 처리 | 오프라인 캐시 + 시각 표시. 푸시 실패 시 죽은 토큰 정리. LLM 실패 시 규칙 폴백 |
| 관측성 | 콜드 스타트 분포 · 프레임 이탈 · OTA 적용률/실패율 · **번들 크기 추이**(번들 분리 판정 근거) · 푸시 도달률 · 알림 클릭률 |
| 시각 일관성 | 웹과 **같은 토큰**. 색·라운드·타이포가 갈라지지 않는다 |

## UX 상태

- **Loading**: 스플래시 → 마지막 캐시를 먼저 그리고 갱신. **네트워크를 기다리지 않는다.**
- **Empty (온보딩 미완료)**: 초대 코드 화면.
- **Offline**: FR-30~33.
- **Blocked (버전 낮음)**: 업데이트 안내 화면. 진행 불가.
- **Blocked (3종 세트 미충족)**: 웹과 같은 회색 박스 + 사유.
- **Degraded (LLM 실패)**: 규칙 기반 문장 + 배지.
- **Error (푸시 권한 거부)**: 홈에 한 줄 안내 — "세금 마감 알림을 받으려면 알림을 허용하세요". **반복해서 묻지 않는다.**
- **Optimistic update**: 없음.

## 정책과 제약

- **공개 스토어 출시를 하지 않는다.** 글로벌 플랜 1-1절(비공개·초대제)이 배포 방식을 결정한다.
- **알림에 금액을 담지 않는다.**
- **주문을 실행하지 않는다.** 웹과 동일.
- **탭을 4개로 만들지 않는다.**
- **오프라인 큐를 만들지 않는다.**
- 웹과 **문구를 공유한다.** 확신 표현·목표주가·인격 평가 금지가 앱에도 그대로 적용된다.
- `MovableGrid`를 모바일에 만들지 않는다. **PC 전용**이다.

## 화면/프론트엔드 영향

| 레이어 | 슬라이스 | 내용 |
|---|---|---|
| `pages` | `home` · `coach` · `assets` · `tax` · `onboarding` · `login` | 3탭 + 내부 스택 |
| `widgets` | `home-briefing` · `coach-console` · `asset-workspace` · `onboarding-flow` | 웹과 같은 조합 |
| `features` | `ask-coach` · `import-ledger` · `accept-invite` · `complete-weekly-plan` · `rate-recommendation` | 웹과 같은 슬라이스 |
| `entities` | `device` | **모바일 전용** — 푸시 토큰 · 버전 |
| `src/native` | 푸시 · 파일 선택 · secure store · 딥링크 | **여기서만 플랫폼 API를 만진다** |
| `packages/tokens` | 신규 (웹과 공유) | |
| `packages/ui-native` | 신규 | |
| `packages/core` | 신규 (뷰모델 타입 · 포맷터) | |

## BFF/API 영향

| Method | Path | 용도 |
|---|---|---|
| POST | `/api/app/device/register` | 푸시 토큰 등록 (기기 정보 + 앱 버전) |
| DELETE | `/api/app/device/:id` | 등록 해제 |
| GET | `/api/app/device/version-gate` | 최소 지원 버전 |
| PATCH | `/api/app/device/notification-prefs` | 알림 타입별 on/off |
| GET | `/api/app/home` | **집계 1콜** (모바일 전용 경로가 아니라 기존 계약) |
| 나머지 | 웹과 동일 | 계약을 두 번 만들지 않는다 |

## 서버/DB/Worker 영향

| Layer | 내용 |
|---|---|
| Server | **`device` 컨텍스트 신규** — 디바이스 등록 · 푸시 발송 · 버전 게이트 · 알림 설정 |
| Server | `notification` 컨텍스트가 `device`의 공개 API로 푸시를 발송한다 |
| DB | `Device`(푸시 토큰 · 플랫폼 · 앱 버전 · 런타임 버전 · 마지막 접속) · `NotificationDelivery`(발송 기록, D-Day 중복 방지) · `NotificationPreference` |
| Worker | `tax-deadline-notify.worker`(F002)가 `device`를 통해 푸시를 보낸다. **D-30/14/7/3/1 각 1회** |
| Worker | 죽은 토큰 정리 (기존 `notification-cleanup.worker` 확장) |

## Trace Matrix

| 요구사항 | 화면 | BFF | 서버 | DB | 검증 |
|---|---|---|---|---|---|
| FR-1~6 | `apps/mobile` | — | — | — | iOS·Android 실기기 기동 |
| FR-10 | `entities/device` | `/device/register` | `device` | `Device` | 등록 후 테스트 푸시 도달 |
| FR-11~13 | 알림 목록 | `/alerts` | `notification` | `NotificationDelivery` | 타입 2종 · D-30 정확히 1회 · 문구 검수 |
| FR-14 | 딥링크 핸들러 | — | — | — | 알림 → 화면 진입 + 재검증 |
| FR-15 | 설정 화면 | `/device/notification-prefs` | `device` | `NotificationPreference` | 끈 상태에서 D-1 도달 |
| FR-16 | — | — | 발송 페이로드 | — | 알림 본문에 금액 0건 |
| FR-20~22 | 버전 게이트 화면 | `/device/version-gate` | `device` | `Device` | 낮은 버전 시뮬레이션 |
| FR-30~33 | 캐시 + 배너 | — | — | — | 기내 모드 테스트 |
| FR-40~43 | secure store · 권한 | — | — | — | 저장소 검사 · 권한 목록 |
| FR-50~54 | — | — | — | — | TestFlight·Play 배포 1회 |

## 수용 기준

- [ ] iOS와 Android 실기기에서 앱이 기동하고 3탭이 동작한다
- [ ] 하단 탭이 정확히 3개다
- [ ] `packages/tokens`가 있고 웹의 색·라운드·타이포가 추출 전후 시각적으로 동일하다
- [ ] `apps/mobile`이 `packages/ui`를 import하지 않는다
- [ ] 푸시 토큰이 등록되고 테스트 푸시가 두 플랫폼에 도달한다
- [ ] 알림 타입이 2종이다
- [ ] **세금 D-Day 알림이 D-30에 정확히 1회** 발송된다 (중복 0건)
- [ ] 알림 본문에 금액이 0건이다
- [ ] 알림 문구에 압박 표현이 0건이다
- [ ] 알림을 누르면 해당 화면으로 딥링크하고 서버 재검증이 일어난다
- [ ] 알림을 끈 상태에서도 D-1이 도달하고, 그 예외가 설정 화면에 명시된다
- [ ] 네이티브 버전이 최소 요구보다 낮으면 업데이트 안내 화면이 뜨고 진행이 막힌다
- [ ] 기내 모드에서 홈이 캐시로 렌더되고 `마지막 갱신 시각`이 보인다
- [ ] 금액 화면 오프라인 배너가 있다
- [ ] 오프라인에서 mutation이 큐에 쌓이지 않고 즉시 실패를 알린다
- [ ] 토큰이 Keychain/Keystore에 있고 `AsyncStorage`에 0건이다
- [ ] 거래소 API 키가 앱에 저장되지 않는다
- [ ] 요청 권한이 최소이고 각 권한에 사유 문구가 있다
- [ ] 폰트 최대 확대에서 금액이 잘리지 않는다
- [ ] 터치 타깃이 전부 44×44 이상이다
- [ ] VoiceOver·TalkBack으로 총자산과 추천 카드가 낭독된다
- [ ] 콜드 스타트가 저가 Android 실기기에서 2s 이내다 (측정값 기록)
- [ ] JS 번들이 4MB 이하다 (측정값 기록)
- [ ] TestFlight·Play 내부 테스트로 배포되고 **공개 출시가 0건**이다
- [ ] OTA로 JS 변경이 스토어 없이 적용된다
- [ ] 롤백을 1회 실행 검증했다
- [ ] 스토어 등록 정보에 투자 조언·수익 보장 표현이 0건이고 면책이 포함된다
- [ ] 추천 카드 3종 세트 게이트가 앱에서도 동작한다 (하나 제거 시 미렌더)

## 검증 계획

1. **실기기 스모크**: iOS 1대 + 저가 Android 1대. 3탭 순회 + 온보딩 완주.
2. **푸시 E2E**: 등록 → 발송 → 도달 → 딥링크 → 재검증. 두 플랫폼.
3. **D-Day 중복 방지**: 같은 날 워커를 3회 실행해 발송 1건 확인.
4. **알림 문구 검수**: 압박 표현·금액 노출.
5. **버전 게이트**: 낮은 버전 시뮬레이션 3케이스(네이티브 낮음 / OTA 런타임 불일치 / 정상).
6. **오프라인**: 기내 모드에서 홈·청구서·세금·대화 각각.
7. **접근성**: 스크린리더 낭독 + 폰트 최대 확대 + 터치 타깃 측정.
8. **성능**: 콜드/웜 스타트, 탭 전환, 리스트 스크롤, 대화 스트리밍 프레임. **릴리스 빌드로** 측정.
9. **보안**: 저장소 grep(토큰·키), 권한 목록, 알림 페이로드 캡처.
10. **배포**: TestFlight·Play 내부 테스트 각 1회 + OTA 1회 + 롤백 1회.
11. **문구 공유 검증**: 웹과 앱의 같은 화면 문구가 일치하는지(i18n 소스 하나).

## Open Questions

- **Apple Developer 계정($99/년)과 Google Play 개발자 계정($25)** 보유 여부. **없으면 배포가 시작되지 않는다.** 착수 전 확인 필수.
- 푸시 구현 방식 — Expo Notifications vs FCM/APNs 직접. **Expo를 쓰기로 한 이상 Expo Notifications가 기본안**이지만, 서버에서 직접 발송하려면 자격 증명 관리 방식이 갈린다.
- TestFlight 내부 테스터(100명, 심사 없음) vs 외부 테스터(경심사). **지인 10명이면 내부로 충분한지** 확인.
- 기준 저가 Android 기기를 무엇으로 정할지. 성능 예산의 측정 기준이 된다.
- 웹과 화면 로직이 두 번 구현된다. `packages/core`로 올릴 범위를 어디까지 할지 — **RN이 React Query를 쓰는지에 따라 `api` 공유 여부가 갈린다**(`RN-REQ-001` Open Question).
- 알림 설정을 서버에 둘지 기기에 둘지. 여러 기기가 있으면 서버가 맞다.
- 세금 마감 시기(12/29~31)와 스냅샷(1/1 00:10)이 걸린 기간의 **릴리스 동결 기간**.

## 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-09-09 | 초안. RN 앱 신설, 푸시 알림 2종, 버전 게이트, 오프라인 정책, TestFlight·Play 내부 테스트 배포. 사용자 결정: *"모바일은 react native이고 apps에 새로 만들면되고 안드로이드 아이폰 다 되게"* |
