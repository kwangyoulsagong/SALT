# F009 슬라이스 0 — 신뢰성 수정 — 회고

영역 회고: `salt-server/.../retrospects/SRV-REQ-025.md` · `bff/.../BFF-REQ-024.md` · `salt-microFe/.../FE-REQ-026.md`

## C04 (2026-09-24)

- 계산이 맞아도 **이름이 거짓이면 결론이 틀린다.** "최대 낙폭"은 "따라 하면 최악일 때 이만큼 빠진다"로 읽힌다
- 같은 단어를 쓰는 스펙이 둘 있었다 — 판단 표본 최저값(오명)과 평가금 곡선 MDD(`FE-REQ-030` FR-101, 올바름). 일괄 치환하지 않고 줄마다 봤다

## Action

- 진짜 MDD 는 F009 슬라이스 1 에서 별도 필드

## C05 (2026-09-24)

- 숫자(채점 상수) 하나 옆에 문장 셋(템플릿 · 프롬프트 · 판단)이 따로 있으면 문장이 먼저 틀어진다 — 표 하나에 숫자와 문장을 같이 두었다
- 스펙(FR-103 "기간은 주입")과 구현("LLM 이 쓰고 검사")이 달랐는데 체크리스트는 통과로 적혀 있었다. **검사가 있다 ≠ 주입한다**

## Action (C05)

- C03 에서 숫자 검증을 지표 단위로 바꿀 때, 템플릿 · 프롬프트의 숫자 출처도 같은 방식(표 하나)으로 모은다

## C06 (2026-09-24)

- 시드가 성적만 부풀린 게 아니라 **워커의 표본 간격까지 막고 있었다**(시드 최신 행 = 어제 자정). 출처 열이 생기자 둘 다 풀렸다
- 열에 기본값을 두지 않은 것이 이번 결정의 핵심이다 — 기본값 `live` 는 다음 백테스트 코드가 조용히 실측이 되는 길이다
