# SRV-REQ-037 F008 가격 변동 범위 API — 체크리스트 (17a-1, 2026-09-24)

| 확인 | 결과 |
|---|---|
| 실 DB — 소유자 · BTC | 1 · 2주 **켜짐**(기준가 115,650,000 · 90% 범위 107.1M ~ 125.4M / 102.7M ~ 128.9M), 3 · 4주 `miscalibrated`. 보유 0.05 BTC → 1주 평가금액 변화 −428,128 ~ +486,647원. `trackRecord.kind: backtest` · 표본 52 · 빗나간 사례 3 · 방향 필드 없음 |
| 실 DB — 비소유자 | `ForecastNotAvailableError` · `NOT_FOUND` → 404 |
| 뷰 조회 | 20회 평균 **5ms**(예산 50ms) |
| 테스트 | `npm test` **328 pass**(+10: 도메인 8 · 유스케이스 2) · `tsc --noEmit` · `npm run build` · eslint · `layer-check` 13파일 exit 0 |
| 금지 필드 | `targetPrice` · `expectedReturn` · `confidence` 0건(테스트) |

## 미검증 · 범위 밖

| 항목 | 사유 | 언제 닫히나 |
|---|---|---|
| HTTP 실측(라우터 · 401 · 400) | 로컬 로그인 토큰 없이 유스케이스로 실측했다 | BFF 슬라이스 18 |
| `.env` 의 `FORECAST_OWNER_EMAILS` | 사용자 계정 이메일을 넣어야 화면에 나온다 | 사용자 설정 |


## 17a-2 — 해설 검증기 · 템플릿 (2026-09-24)

| 확인 | 결과 |
|---|---|
| 말투 검증기 `languageGuard` | 확신 · 명령형 매매 지시 · 목표가 · 지어낸 숫자. 면책 부정문("보장하지 않습니다")은 통과 |
| 규칙 문장 전수 검사 | **위반 6건을 찾아 고쳤다** — "분할 매수를 고려하세요" · "비중 축소를 검토하세요" · "비중 재조정을 우선 검토하세요"(`explain.ts`), "단계형 익절을 검토하세요"(`profitPlan.ts`), "진입 조건을 다시 검토하세요"(`preflight.ts`), "…익절 구간을 기록하세요"(`AnalyzeTradingBehavior.ts`). 이제 테스트가 `policy/` · `application/` 문자열 전부를 검사한다 |
| LLM 출력 검증 | 문장마다 검사, 걸린 칸은 템플릿. LLM 실패 → 전부 템플릿(해설이 비지 않는다). 중단은 템플릿으로 덮지 않는다 |
| 응답 추가 필드 | `source`(`llm` · `llm_checked` · `template`) · `droppedSentences` — 추가만, 기존 필드 불변 |
| **실측 — 지금 Gemini 출력** | 3종목(BTC 장기 · ETH 단타 · XRP 장기) **24문장 중 걸림 0** — 프롬프트 규칙이 지금은 지켜지고 있다. 표본이 작아 검증기는 안전망으로 둔다 |
| 테스트 | `npm test` **336 pass**(+8) · `tsc` · eslint |

| 미검증 · 범위 밖 | 사유 | 언제 닫히나 |
|---|---|---|
| 블라인드 A/B 20건(템플릿 vs Gemini) | 사람 평가가 필요하다 | 사용자 평가 |
| 화면의 `source: template` 배지 | 프론트 미착수 | 슬라이스 19 |
| 한국어 조사(은/는) 처리 | 템플릿이 조사를 붙이지 않는 문장 모양으로 피했다 | 템플릿 문장이 늘 때 |

## F009 슬라이스 0 — C03 숫자 검증 (FR-7a, 2026-09-24, `feat/f009-slice0-reliability`)

| 진단 재현 문장 | 입력 | 옛 검사 | 이제 |
|---|---|---|---|
| 수익률은 12%예요. | 없음 | 통과 | `unverified_number` |
| 가격이 20% 상승했어요. | −20% | 통과 | `unverified_number`(방향) |
| 금리가 상승해서 가격이 하락했어요. | 없음 | 통과 | `unsupported_causal` |
| 31% 올랐어요. | RSI 31 | 통과 | `unverified_number`(단위) |

| 확인 | 결과 |
|---|---|
| 통과해야 하는 것 | 반올림(2.34 → 2.3%) · 같은 방향(−20 → "20% 하락") · "RSI 31점" · "두 가지, 3개" · "1억 1,565만 원" = 115,650,000 · 근거 숫자가 있는 인과 — 전부 통과(테스트) |
| 템플릿 해설 | 자기 검증 통과(기존 테스트 유지) — 템플릿이 쓰는 "−1.23%" · "3,120억 원" · "24시간" 이 사실과 맞는다 |
| 날짜 | "2026-09-24" 의 하이픈을 부호로 읽지 않는다 |
| 게이트 | `npm run build` · `npm test` **361 / 0**(+5) · `eslint .` · `layer-check` |

| 미검증 | 사유 | 언제 닫히나 |
|---|---|---|
| 지표 이름 대조("수익률 2.3%" 가 변동률 2.3% 인가) | 근거가 자유 문자열 — 구조가 없다 | C01(서버 사실 스냅샷) |
| 실제 Gemini 출력에서 버려지는 비율 | 새 규칙이 더 엄격하다(특히 인과). 템플릿으로 떨어지는 비율을 안 쟀다 | 사용자 해설 몇 건 실측 · 관측성 계측 |
| 숫자를 렌더러가 넣고 LLM 은 사실 ID 만 쓰는 방식 | 진단의 최종안. 구조화된 사실이 먼저 있어야 한다 | C01 뒤 판단 |
