# DB-REQ-031 회고

## 슬라이스 1 (2026-09-24)

- 정밀도를 규칙(`performance-database.md` §1)보다 먼저 정했다가 되돌렸다. 로컬에만 적용된 상태라 롤백 SQL → 재생성으로 **마이그레이션 한 벌**을 지켰다. 새 금액 컬럼은 규칙 표를 먼저 본다
- 예산을 값 · 단위로 나누고 짝을 CHECK 로 묶었다. 서비스 검증만 있으면 반쪽 값이 "예산 있음"으로 읽힐 수 있다
- `prisma format` 이 관계없는 모델 정렬까지 바꾼다 — 그 부분은 되돌려 diff 를 이 REQ 에 한정했다

## Action

- 새 테이블 인덱스의 `EXPLAIN` 은 행이 쌓이는 슬라이스 4 에서
