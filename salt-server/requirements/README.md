# Server Requirements

서버 API, 모듈, DB/worker 영향 요구사항과 검증 리포트를 관리한다.

## Directory

```text
salt-server/requirements/
  specs/
    to-do/
    in-progress/
    done/
  reports/
    checklists/
    retrospects/
```

작업 중인 요구사항과 API spec은 `specs/in-progress/`에 둔다.
